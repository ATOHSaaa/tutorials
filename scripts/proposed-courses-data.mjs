function lesson(id, title, description, sections) {
  return { id, title, description, sections }
}
function sec(heading, content, extra = {}) {
  return { heading, content, ...extra }
}

export const PROPOSED_COURSES = [
  {
    slug: 'http',
    componentName: 'HttpTutorial',
    courseTitle: 'HTTP',
    sidebarTitle: 'HTTP チュートリアル',
    heroAccent: 'HTTP / Web API',
    heroSub: 'ブラウザとサーバーがどう通信するかを、HTTP の仕組みから REST API まで10レッスンで学びます。',
    feature1: 'リクエスト・レスポンスの流れやステータスコードをデモで確認できます。',
    icon: '🌐',
    logoIcon: '🌐',
    section: 'core',
    subtitle: 'Web の通信の基礎',
    description: 'HTTP メソッド、ヘッダー、REST API、fetch、CORS まで。Web 開発の土台となる HTTP を10レッスンで学びます。',
    gradient: 'linear-gradient(135deg, #2563eb, #7c3aed)',
    lessons: [
      lesson('intro', 'HTTP とは？', 'HTTP の役割と Web 通信の基本構造を理解します', [
        sec('HTTP の位置づけ', 'HTTP（HyperText Transfer Protocol）は、ブラウザとサーバーがデータをやり取りするための「約束事」です。URL を開く、フォームを送信する、API を呼ぶ——これらはすべて HTTP リクエストとレスポンスの往復で成り立っています。\n\nHTTP はステートレス（状態を持たない）プロトコルです。サーバーは前のリクエストを覚えていません。ログイン状態やカートの中身は、Cookie やセッション、トークンなど別の仕組みで管理します。', { tip: 'デモでブラウザがサーバーへリクエストを送る流れを追ってみてください。' }),
        sec('リクエストとレスポンス', '通信は常に「クライアントがリクエストを送り、サーバーがレスポンスを返す」という形です。リクエストにはメソッド（GET など）、URL、ヘッダー、本文（ボディ）が含まれます。レスポンスにはステータスコード、ヘッダー、本文が含まれます。\n\n開発者ツールの Network タブを開けば、実際の HTTP 通信をリアルタイムで確認できます。どのリクエストが遅いか、どのヘッダーが付いているかを見る習慣が、デバッグの第一歩になります。'),
        sec('HTTP のバージョン', '現在主流は HTTP/1.1 と HTTP/2、HTTP/3 です。HTTP/1.1 は1接続あたり1リクエストが基本でしたが、HTTP/2 は多重化により1接続で複数リクエストを並列処理できます。HTTP/3 は QUIC ベースで、モバイル環境での接続安定性が向上しています。\n\nフロントエンド開発者はバージョンを直接選ぶことは少ないですが、「なぜ HTTP/2 で速くなるのか」を知ると、リソース分割やドメインシャーディングの設計判断がしやすくなります。'),
      ]),
      lesson('request-response', 'リクエストとレスポンスの構造', 'HTTP メッセージの各部分を分解して理解します', [
        sec('リクエストライン', 'リクエストの最初の行は「メソッド + パス + HTTP バージョン」です。例: `GET /api/users HTTP/1.1`。クエリ文字列はパスに含まれます（`?page=2&limit=10`）。\n\nメソッドは「何をしたいか」を表し、パスは「どのリソースに対してか」を表します。REST API ではパスの設計が API の使いやすさを大きく左右します。', { code: 'GET /api/articles?tag=javascript&sort=desc HTTP/1.1\nHost: example.com\nAccept: application/json\nAuthorization: Bearer eyJhbGciOi...' }),
        sec('レスポンスの構造', 'レスポンスも同様に、ステータスライン・ヘッダー・ボディの3部構成です。ステータスラインは `HTTP/1.1 200 OK` のように、プロトコルバージョン・ステータスコード・理由フレーズを含みます。\n\nボディの形式は Content-Type ヘッダーで示されます。JSON API では `application/json`、HTML ページでは `text/html` が一般的です。Content-Type を正しく設定しないと、ブラウザが JSON をテキストとして表示するなどの問題が起きます。'),
        sec('ボディの扱い', 'GET リクエストは通常ボディを持ちません。POST・PUT・PATCH では JSON やフォームデータをボディに載せます。`Content-Length` や `Transfer-Encoding: chunked` でボディのサイズや分割送信を伝えます。\n\nfetch API では `body: JSON.stringify({ name: "太郎" })` と `headers: { "Content-Type": "application/json" }` をセットで指定するのが基本パターンです。', { code: 'const res = await fetch("/api/users", {\n  method: "POST",\n  headers: { "Content-Type": "application/json" },\n  body: JSON.stringify({ name: "太郎", email: "taro@example.com" }),\n});\nconst user = await res.json();' }),
      ]),
      lesson('methods', 'HTTP メソッド', 'GET・POST・PUT・DELETE などメソッドの意味と使い分けを学びます', [
        sec('主要メソッド一覧', '• GET — リソースの取得（副作用なし、キャッシュ可能）\n• POST — リソースの新規作成\n• PUT — リソースの全体置換\n• PATCH — リソースの部分更新\n• DELETE — リソースの削除\n\nメソッドは「動詞」、URL は「名詞（リソース）」と考えると RESTful な設計に近づきます。`/deleteUser` のような動詞入り URL は避け、`DELETE /users/123` のようにします。'),
        sec('GET と POST の違い', 'GET はデータ取得専用で、パラメータはクエリ文字列に載せます。ブラウザの戻るボタンやブックマーク、CDN キャッシュの対象になりやすいのが特徴です。\n\nPOST はサーバー側の状態を変える操作に使います。フォーム送信、ユーザー登録、決済処理など。同じ POST を繰り返すと二重登録になる可能性があるため、冪等性（何度実行しても同じ結果）には注意が必要です。', { tip: 'フォームの method 属性が GET と POST で挙動が変わることをデモで確認してみてください。' }),
        sec('PUT / PATCH / DELETE', 'PUT はリソース全体を送って置き換えます。`{ name, email, role }` すべてを含める必要があります。PATCH は変更したフィールドだけ送る部分更新です。実務では PATCH の方がよく使われます。\n\nDELETE はリソースの削除を意味しますが、実際には論理削除（deleted_at フラグ）を PATCH で行う API も多いです。API 設計時は「本当に DELETE メソッドを公開するか」をチームで決めましょう。', { code: '// PATCH — 名前だけ変更\nawait fetch("/api/users/42", {\n  method: "PATCH",\n  headers: { "Content-Type": "application/json" },\n  body: JSON.stringify({ name: "花子" }),\n});' }),
      ]),
      lesson('status-codes', 'ステータスコード', '200・404・500 など、レスポンスの意味を読み解きます', [
        sec('ステータスコードの分類', 'ステータスコードは3桁の数字で、先頭1桁がカテゴリを示します。\n\n• 1xx — 情報（ほぼ見ない）\n• 2xx — 成功（200 OK、201 Created、204 No Content）\n• 3xx — リダイレクト（301 Moved、302 Found、304 Not Modified）\n• 4xx — クライアントエラー（400 Bad Request、401 Unauthorized、403 Forbidden、404 Not Found、422 Unprocessable）\n• 5xx — サーバーエラー（500 Internal Server Error、502 Bad Gateway、503 Service Unavailable）'),
        sec('よく使うコードの実例', '200 は一般的な成功。201 は POST でリソースが作成されたとき。204 は DELETE 成功でボディがないとき。\n\n401 は「認証が必要」、403 は「認証済みだが権限がない」という違いがあります。フロントエンドでは 401 ならログイン画面へ、403 ならエラーメッセージ表示と分岐するのが一般的です。', { code: 'if (res.status === 401) {\n  router.push("/login");\n} else if (res.status === 403) {\n  showError("この操作は許可されていません");\n} else if (!res.ok) {\n  showError(`エラー: ${res.status}`);\n}' }),
        sec('エラーハンドリングの設計', 'API は意味のあるステータスコードとエラーメッセージを返すべきです。すべて 200 で `{ success: false }` を返す設計は、HTTP の恩恵（キャッシュ、ミドルウェアの自動処理）を活かせません。\n\n422 Unprocessable Entity はバリデーションエラー向きです。`{ errors: { email: ["形式が不正です"] } }` のような構造で、フォームの各フィールドにエラーを表示できます。', { tip: 'デモで意図的に 404 や 500 を返す API を叩き、ブラウザの挙動を確認してみてください。' }),
      ]),
      lesson('headers', 'HTTP ヘッダー', 'Content-Type、Authorization、Cache-Control など重要ヘッダーを学びます', [
        sec('ヘッダーの役割', 'ヘッダーはリクエストやレスポンスの「メタデータ」です。本文の形式、認証情報、キャッシュ方針、CORS 関連情報などを伝えます。\n\nカスタムヘッダーは `X-` プレフィックス（非推奨になりつつある）や、仕様で定義された名前を使います。`Authorization`、`Content-Type`、`Accept` は API 開発で毎日触るヘッダーです。'),
        sec('Content-Type と Accept', 'Content-Type は「送るデータの形式」、Accept は「受け取りたい形式」を示します。JSON API では `Content-Type: application/json` が標準です。\n\n`Accept: application/json` を付けると、サーバーが JSON を返すことを期待できます。HTML フォーム送信では `application/x-www-form-urlencoded` や `multipart/form-data`（ファイルアップロード時）を使います。', { code: 'const res = await fetch("/api/search", {\n  headers: {\n    "Content-Type": "application/json",\n    "Accept": "application/json",\n  },\n  body: JSON.stringify({ q: "TypeScript" }),\n});' }),
        sec('Authorization と Cookie', 'Authorization ヘッダーは Bearer トークン（JWT など）を載せるのが一般的です。`Authorization: Bearer <token>` の形式です。\n\nCookie はブラウザが自動で送るヘッダーです。`Set-Cookie` でサーバーが設定し、`Cookie` でクライアントが返します。HttpOnly・Secure・SameSite 属性はセキュリティ上重要です（認証チュートリアルで詳しく学びます）。'),
      ]),
      lesson('rest', 'REST API の設計', 'RESTful な API 設計の原則と実践パターンを学びます', [
        sec('REST の基本原則', 'REST（Representational State Transfer）は、リソースを URL で表現し、HTTP メソッドで操作する設計スタイルです。`/users` がユーザーコレクション、`/users/123` が特定ユーザーという命名が基本です。\n\nリソースは名詞で表し、操作は HTTP メソッドに委ねます。`/getUsers` や `/createUser` のような RPC 風 URL は REST では避けます。'),
        sec('コレクションとリソース', 'コレクション URL（`/articles`）への GET は一覧、POST は新規作成。リソース URL（`/articles/42`）への GET は詳細、PATCH は更新、DELETE は削除。\n\nネストは深くしすぎないのが鉄則です。`/users/123/posts/456/comments` は4段階で上限に近い。`/comments/789` とフラットにする方がシンプルなことが多いです。', { code: 'GET    /api/articles          → 一覧\nPOST   /api/articles          → 新規作成\nGET    /api/articles/:id      → 詳細\nPATCH  /api/articles/:id      → 更新\nDELETE /api/articles/:id      → 削除' }),
        sec('ページネーションとフィルタ', '一覧 API にはページネーションが必須です。`?page=2&limit=20` や cursor ベース（`?cursor=abc123`）が一般的です。レスポンスには `total` や `nextCursor` を含めます。\n\nフィルタはクエリパラメータで表現します。`?status=published&tag=react&sort=-createdAt`。複雑な検索は POST `/api/articles/search` にボディで条件を送るパターンもあります。', { tip: 'デモで REST API の URL 設計をシミュレートし、直感的かどうか検証してみてください。' }),
      ]),
      lesson('fetch', 'fetch API', 'ブラウザから HTTP リクエストを送る fetch の実践を学びます', [
        sec('fetch の基本', 'fetch はブラウザ標準の HTTP クライアント API です。Promise を返すため async/await と相性が良く、XMLHttpRequest の後継として広く使われています。\n\n`fetch(url)` は GET リクエストを送り、Response オブジェクトを返します。`res.json()` で JSON をパース、`res.text()` でテキストを取得します。', { code: 'async function loadUsers() {\n  const res = await fetch("/api/users");\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  const users = await res.json();\n  return users;\n}' }),
        sec('オプションとエラーハンドリング', '第2引数で method、headers、body、credentials などを指定します。`credentials: "include"` で Cookie を送るクロスオリジンリクエストが可能になります（CORS 設定が必要）。\n\nfetch はネットワークエラーでのみ reject します。404 や 500 でも resolve するため、必ず `res.ok` または `res.status` をチェックしてください。これは初心者がよくハマるポイントです。', { code: 'try {\n  const res = await fetch("/api/data", { method: "DELETE" });\n  if (res.status === 204) return null;\n  if (!res.ok) throw new Error(await res.text());\n  return await res.json();\n} catch (err) {\n  console.error("リクエスト失敗:", err);\n}' }),
        sec('AbortController', '長時間かかるリクエストや、コンポーネントのアンマウント時にキャンセルするには AbortController を使います。React の useEffect クリーンアップと組み合わせるのが定番パターンです。', { code: 'const controller = new AbortController();\nfetch("/api/slow", { signal: controller.signal })\n  .then(res => res.json())\n  .catch(err => {\n    if (err.name === "AbortError") console.log("キャンセルされました");\n  });\n// キャンセル: controller.abort();' }),
      ]),
      lesson('cors', 'CORS', 'クロスオリジンリクエストの制約と対処法を学びます', [
        sec('同一オリジンポリシー', 'ブラウザはセキュリティのため、JavaScript から別オリジン（ドメイン・ポート・プロトコルの組み合わせ）へのリクエストを制限します。`https://app.example.com` から `https://api.example.com` への fetch は「クロスオリジン」です。\n\n`<img src>` や `<script src>` は従来制限が緩いですが、fetch や XMLHttpRequest では CORS（Cross-Origin Resource Sharing）のルールが適用されます。'),
        sec('プリフライトリクエスト', 'POST で JSON を送る、Authorization ヘッダーを付けるなど「単純リクエスト」でない場合、ブラウザは先に OPTIONS リクエスト（プリフライト）を送ります。サーバーが `Access-Control-Allow-Origin` などを返せば、本番リクエストが実行されます。\n\n開発中に「CORS エラー」が出たら、まずサーバー側の CORS 設定を確認します。フロントだけで解決できる問題ではありません。', { code: '// サーバー側（Express の例）\napp.use(cors({\n  origin: "https://app.example.com",\n  credentials: true,\n  methods: ["GET", "POST", "PUT", "DELETE"],\n}));' }),
        sec('開発時の対処', 'ローカル開発では Vite の proxy 設定で `/api` をバックエンドに転送する方法が安全です。ブラウザからは同一オリジンに見えるため CORS が発生しません。\n\n本番では API サーバーが正しい `Access-Control-Allow-Origin` を返す必要があります。`*` は credentials 付きリクエストでは使えません。', { tip: 'デモでプリフライトの OPTIONS リクエストが Network タブに表示されることを確認してみてください。' }),
      ]),
      lesson('caching', 'HTTP キャッシュ', 'Cache-Control によるキャッシュ戦略を学びます', [
        sec('キャッシュの仕組み', 'HTTP キャッシュは、同じリソースへの再リクエストを省略し、表示速度を上げる仕組みです。ブラウザキャッシュと CDN キャッシュの2層があります。\n\nサーバーは `Cache-Control` ヘッダーで「どれくらいの間キャッシュしていいか」を指示します。`max-age=3600` は1時間、`no-store` はキャッシュ禁止です。'),
        sec('Cache-Control の主要ディレクティブ', '• `public` — CDN もキャッシュ可能\n• `private` — ブラウザのみ（ユーザー固有データ向き）\n• `no-cache` — キャッシュ前にサーバーへ再確認\n• `no-store` — 一切保存しない\n• `max-age=秒` — 有効期限\n• `immutable` — 内容が変わらない（ハッシュ付きファイル名向き）', { code: '# 静的アセット（ビルド時ハッシュ付き）\nCache-Control: public, max-age=31536000, immutable\n\n# API（短め）\nCache-Control: public, max-age=60, s-maxage=300\n\n# ユーザー固有\nCache-Control: private, no-store' }),
        sec('ETag と条件付きリクエスト', 'ETag はリソースのバージョンを示す識別子です。クライアントが `If-None-Match: "abc123"` を送り、サーバーが変更なければ 304 Not Modified を返します。ボディなしで「変わってない」と伝えられるため、帯域を節約できます。\n\n静的ファイルは長期キャッシュ + ファイル名にハッシュ（`app.a1b2c3.js`）が最強の組み合わせです。HTML は短い max-age か no-cache が一般的です。'),
      ]),
      lesson('next-steps', '次のステップ', 'HTTP の学習を続けるための道筋を確認します', [
        sec('次に学ぶこと', '① Node.js 入門 — サーバー側で HTTP を扱う\n② 認証入門 — Cookie・JWT・OAuth\n③ Webセキュリティ — XSS・CSRF・HTTPS\n④ GraphQL 入門 — REST の代替アプローチ'),
        sec('学習の道筋', 'HTTP は Web 開発の共通言語です。fetch で API を呼ぶたびに、メソッド・ステータスコード・ヘッダーの知識が活きます。Network タブを開く習慣をつけ、実際の通信を観察し続けましょう。\n\nおめでとうございます！HTTP 入門をすべて学びました 🎉', { tip: '次は Node.js チュートリアルでサーバー側の HTTP を体験してみてください。' }),
        sec('実践チェックリスト', '• API を呼ぶとき res.ok をチェックしているか\n• 適切な HTTP メソッドを使っているか\n• Content-Type を正しく設定しているか\n• CORS エラーの原因をサーバー/クライアントどちらか特定できるか\n• Cache-Control を意識してパフォーマンスを改善できるか'),
      ]),
    ],
  },
  {
    slug: 'node',
    componentName: 'NodeTutorial',
    courseTitle: 'Node.js',
    sidebarTitle: 'Node.js チュートリアル',
    heroAccent: 'Node.js',
    heroSub: 'JavaScript をサーバー側で動かす Node.js。ファイル操作から HTTP サーバーまで10レッスンで学びます。',
    feature1: 'イベントループや非同期処理の流れをデモで体験できます。',
    icon: '🟢',
    logoIcon: '🟢',
    section: 'core',
    subtitle: 'サーバー側 JavaScript',
    description: 'Node.js の基本、モジュール、fs、HTTP サーバー、非同期処理まで。バックエンド開発の第一歩を10レッスンで学びます。',
    gradient: 'linear-gradient(135deg, #339933, #68a063)',
    lessons: [
      lesson('intro', 'Node.js とは？', 'Node.js の特徴とブラウザ JS との違いを理解します', [
        sec('Node.js の位置づけ', 'Node.js は Chrome の V8 エンジン上で JavaScript を実行するランタイムです。2009年の登場以降、フロントエンドと同じ言語でサーバー・CLI・ツールを書けるようになり、Web 開発の景色が変わりました。\n\nブラウザには DOM や window がありませんが、代わりに fs（ファイル）、http（ネットワーク）、process（環境変数）などのモジュールが使えます。'),
        sec('イベント駆動とノンブロッキング I/O', 'Node.js の核心はイベントループです。ファイル読み込みや DB クエリの待ち時間に CPU を止めず、他の処理を進めます。1スレッドで多数の接続を扱えるため、I/O 中心の Web API に向いています。\n\nCPU を大量に使う画像処理や動画エンコードは、Worker Threads や別サービスに任せるのが一般的です。', { tip: 'デモで同期処理と非同期処理の実行順序の違いを確認してみてください。' }),
        sec('Node.js の用途', '• REST API サーバー（Express、Fastify）\n• SSR（Next.js のサーバー側）\n• CLI ツール（eslint、prettier、vite）\n• ビルドツール・バンドラー\n• WebSocket サーバー\n\nフロントエンド開発者にとって、Node.js は「開発ツールの実行環境」でも「API を書く環境」でもあります。'),
      ]),
      lesson('setup', 'セットアップ', 'Node.js のインストールとプロジェクト初期化を学びます', [
        sec('インストール方法', '公式サイト（nodejs.org）から LTS 版をインストールするのが基本です。macOS では nvm（Node Version Manager）で複数バージョンを切り替えるのが開発者に人気です。\n\n`node -v` と `npm -v` でバージョンを確認します。チーム開発では `.nvmrc` や `engines` フィールドでバージョンを揃えます。', { code: '# nvm の例\nnvm install 20\nnvm use 20\nnode -v  # v20.x.x\nnpm -v   # 10.x.x' }),
        sec('プロジェクトの作成', '`npm init -y` で package.json を生成し、プロジェクトのルートを作ります。TypeScript を使う場合は `npm init -y` の後に tsconfig.json を追加します。\n\n`"type": "module"` を package.json に書くと ES Modules（import/export）がデフォルトになります。近年の新規プロジェクトは ESM を選ぶことが多いです。', { code: 'mkdir my-api && cd my-api\nnpm init -y\n# package.json に追加\n# "type": "module"' }),
        sec('実行とデバッグ', '`node index.js` でスクリプトを実行します。開発中は `node --watch index.js`（Node 18+）でファイル変更時に自動再起動できます。nodemon も広く使われています。\n\nVS Code のデバッガーでブレークポイントを置き、変数の値を確認する習慣をつけましょう。console.log だけに頼らない開発ができます。'),
      ]),
      lesson('modules', 'モジュールシステム', 'CommonJS と ES Modules の import/export を学びます', [
        sec('ES Modules（推奨）', 'ES Modules は `import` と `export` を使う標準形式です。ブラウザと同じ構文で、Tree Shaking も効きやすく、現代の Node.js プロジェクトのデフォルトです。\n\n`export function greet(name) { ... }` で公開し、`import { greet } from "./utils.js"` で読み込みます。拡張子 `.js` を省略できない点に注意（TypeScript では設定で解決）。', { code: '// utils.js\nexport function add(a, b) { return a + b; }\nexport default function main() { console.log("start"); }\n\n// index.js\nimport main, { add } from "./utils.js";' }),
        sec('CommonJS（レガシー）', 'CommonJS は `require()` と `module.exports` を使います。古い npm パッケージや Node.js の歴史的なコードに残っています。\n\n`const fs = require("fs")` の形式です。ESM プロジェクトから CJS パッケージを import することはできますが、逆は難しい場合があります。新規コードは ESM を使いましょう。'),
        sec('組み込みモジュール', 'Node.js には標準モジュールが付属しています。`node:fs`、`node:path`、`node:http` のように `node:` プレフィックスで明示的に import するのが推奨されます。\n\nサードパーティパッケージと名前が衝突しないよう、組み込みモジュールには `node:` を付ける習慣をつけてください。', { code: 'import fs from "node:fs/promises";\nimport path from "node:path";\nimport { fileURLToPath } from "node:url";\n\nconst __dirname = path.dirname(fileURLToPath(import.meta.url));' }),
      ]),
      lesson('fs', 'ファイル操作', 'fs モジュールでファイルの読み書きを学びます', [
        sec('非同期ファイル I/O', 'fs/promises の `readFile` と `writeFile` は Promise を返すため async/await と相性が良いです。同期的な `readFileSync` はスクリプトや起動時のみに限定し、通常の API 処理では使いません。\n\nファイルが存在しない場合は ENOENT エラーが throw されます。try/catch で捕捉し、適切な HTTP ステータス（404 など）に変換します。', { code: 'import fs from "node:fs/promises";\n\nconst data = await fs.readFile("config.json", "utf-8");\nconst config = JSON.parse(data);\n\nawait fs.writeFile("output.txt", "Hello, Node!", "utf-8");' }),
        sec('ディレクトリ操作', '`mkdir` でディレクトリ作成、`readdir` で一覧取得、`stat` でファイル情報取得が基本操作です。`{ recursive: true }` オプションで親ディレクトリもまとめて作成できます。\n\nアップロード機能では、一時ディレクトリに保存してから本番パスへ移動するパターンがよく使われます。', { code: 'await fs.mkdir("uploads/images", { recursive: true });\nconst files = await fs.readdir("uploads");\nconst info = await fs.stat("uploads/images/photo.jpg");\nconsole.log(info.size, info.mtime);' }),
        sec('パスの扱い', 'path モジュールで OS 差異を吸収します。`path.join("dir", "file.txt")` は `/` と `\\` を正しく結合し、`path.resolve` は絶対パスを返します。\n\nユーザー入力のファイル名をそのまま使うとパストラバーサル攻撃の危険があります。`path.basename` でファイル名だけ取り出し、許可リストで検証しましょう。', { tip: 'デモで相対パスと絶対パスの違いを確認してみてください。' }),
      ]),
      lesson('http-server', 'HTTP サーバー', 'node:http でシンプルな Web サーバーを作ります', [
        sec('最小の HTTP サーバー', 'Node.js 組み込みの http モジュールで、追加ライブラリなしにサーバーを立てられます。本番では Express や Fastify を使いますが、HTTP の仕組みを理解するには組み込みモジュールが最適です。\n\n`createServer` にリクエストハンドラを渡し、`listen` でポートを開きます。', { code: 'import http from "node:http";\n\nconst server = http.createServer((req, res) => {\n  res.writeHead(200, { "Content-Type": "text/plain; charset=utf-8" });\n  res.end("Hello, Node.js!");\n});\n\nserver.listen(3000, () => console.log("http://localhost:3000"));' }),
        sec('ルーティングの基本', 'req.url と req.method でパスとメソッドを判定し、分岐します。本格的なルーティングはフレームワークに任せますが、仕組みは同じです。\n\nJSON API では `Content-Type: application/json` を設定し、`JSON.stringify` でボディを返します。', { code: 'if (req.method === "GET" && req.url === "/api/health") {\n  res.writeHead(200, { "Content-Type": "application/json" });\n  res.end(JSON.stringify({ status: "ok" }));\n} else {\n  res.writeHead(404);\n  res.end("Not Found");\n}' }),
        sec('リクエストボディの読み取り', 'POST のボディはストリームとして届きます。chunk を結合してから JSON.parse します。Express では `express.json()` ミドルウェアがこれを自動化します。\n\n大きなボディにはサイズ制限を设けるべきです。メモリに全部載せる前に、ストリーム処理や multer などのライブラリを検討しましょう。'),
      ]),
      lesson('env', '環境変数', 'process.env と .env ファイルの管理を学びます', [
        sec('process.env', '環境変数は `process.env.PORT` のようにアクセスします。本番と開発で DB の接続先や API キーを切り替える標準的な方法です。\n\n`PORT` や `NODE_ENV` は Node.js エコシステムで広く使われる名前です。`NODE_ENV=production` で本番モードを示します。', { code: 'const port = process.env.PORT ?? 3000;\nconst dbUrl = process.env.DATABASE_URL;\n\nif (!dbUrl) {\n  throw new Error("DATABASE_URL が設定されていません");\n}' }),
        sec('.env ファイル', 'dotenv パッケージで `.env` ファイルを読み込みます。`.env` は Git にコミットせず、`.env.example` にキー名だけ記載して共有します。\n\n本番環境ではプラットフォーム（Vercel、Railway など）の環境変数設定 UI を使い、ファイルではなく環境変数として注入します。', { code: '# .env\nPORT=3000\nDATABASE_URL=postgresql://localhost:5432/mydb\nJWT_SECRET=your-secret-here\n\n# index.js\nimport "dotenv/config";' }),
        sec('シークレット管理', 'API キー・DB パスワード・JWT シークレットは絶対にコードにハードコードしません。漏洩時は即座にローテーション（再発行）します。\n\n`.gitignore` に `.env` を必ず追加し、pre-commit フックでシークレット検出ツール（gitleaks など）を使うのも有効です。'),
      ]),
      lesson('async', '非同期処理', 'Promise、async/await、エラーハンドリングを学びます', [
        sec('Promise と async/await', 'Node.js の I/O はほぼすべて非同期です。コールバック地獄を避けるため、Promise と async/await を使います。\n\n`async function` は常に Promise を返し、`await` は Promise の解決を待ちます。try/catch で同期的なスタイルでエラーハンドリングできます。', { code: 'async function fetchUser(id) {\n  try {\n    const res = await fetch(`https://api.example.com/users/${id}`);\n    if (!res.ok) throw new Error(`HTTP ${res.status}`);\n    return await res.json();\n  } catch (err) {\n    console.error("ユーザー取得失敗:", err);\n    throw err;\n  }\n}' }),
        sec('並列処理', '複数の非同期処理を並列実行するには `Promise.all` を使います。順番に await すると遅くなるため、独立した処理は並列化しましょう。\n\n1つが失敗したら全体を失敗させる `Promise.all`、最初の1つだけ欲しい `Promise.race`、すべて完了を待つ `Promise.allSettled` を使い分けます。', { code: 'const [users, posts] = await Promise.all([\n  fetchUsers(),\n  fetchPosts(),\n]);\n\nconst results = await Promise.allSettled(tasks);\nresults.forEach(r => {\n  if (r.status === "rejected") console.error(r.reason);\n});' }),
        sec('イベントループの注意点', '同期的な重い処理（大きな JSON の parse、暗号化ループ）はイベントループをブロックし、他のリクエストを止めます。\n\nCPU バウンドな処理は Worker Threads やキュー（BullMQ など）に逃がす設計を検討してください。', { tip: 'デモで await の前後で console.log の出力順序を確認してみてください。' }),
      ]),
      lesson('express-intro', 'Express 入門', 'Express でルーティングとミドルウェアを学びます', [
        sec('Express とは', 'Express は Node.js 最大手の Web フレームワークです。ルーティング、ミドルウェア、テンプレート連携などを簡潔な API で提供します。\n\n`npm install express` で追加し、数行で REST API を立ち上げられます。Fastify や Hono など軽量な代替もありますが、Express の知識はどこでも活きます。', { code: 'import express from "express";\nconst app = express();\napp.use(express.json());\n\napp.get("/api/users", (req, res) => {\n  res.json([{ id: 1, name: "太郎" }]);\n});\n\napp.listen(3000);' }),
        sec('ミドルウェア', 'ミドルウェアはリクエスト→レスポンスの途中で実行される関数です。`app.use(express.json())` はボディをパースし、`req.body` を使えるようにします。\n\n認証チェック、ログ出力、CORS 設定もミドルウェアで実装します。順序が重要——json パーサーの後にルートハンドラを置きます。', { code: 'app.use((req, res, next) => {\n  console.log(`${req.method} ${req.url}`);\n  next(); // 次のミドルウェアへ\n});\n\napp.use("/api", authMiddleware);\napp.use("/api", apiRouter);' }),
        sec('ルーターの分割', '大きなアプリでは `express.Router()` でルートをファイル分割します。`routes/users.js`、`routes/posts.js` のようにモジュール化し、`app.use("/api/users", usersRouter)` でマウントします。\n\nコントローラー・サービス層にロジックを分離すると、テストと保守が楽になります。'),
      ]),
      lesson('npm-scripts', 'npm スクリプト連携', 'package.json の scripts と Node.js を組み合わせます', [
        sec('scripts の定義', '`"scripts": { "dev": "node --watch index.js", "start": "node index.js" }` で `npm run dev` として実行できます。チーム全員が同じコマンドで開発を始められます。\n\n`npm start` は `start` スクリプトの省略形です。`npm test` は `test` スクリプトの省略形です。'),
        sec('開発と本番の切り替え', 'cross-env で OS 差異のない環境変数設定、`concurrently` で複数プロセスの同時起動がよく使われます。\n\n`"dev": "node --watch --env-file=.env index.js"` のように Node 20+ の組み込み .env サポートも活用できます。', { code: '"scripts": {\n  "dev": "node --watch index.js",\n  "start": "NODE_ENV=production node index.js",\n  "lint": "eslint .",\n  "test": "node --test"\n}' }),
        sec('次のステップへの橋渡し', 'Node.js の基礎ができたら、npm 入門でパッケージ管理を深掘りし、Prisma や Supabase で DB 連携を学びましょう。フロントエンドとバックエンドを同じ言語で書けるのが Node.js の大きな魅力です。'),
      ]),
      lesson('next-steps', '次のステップ', 'Node.js の学習を続けるための道筋を確認します', [
        sec('次に学ぶこと', '① npm 入門 — パッケージ管理の詳細\n② 認証入門 — JWT・セッション\n③ Prisma 入門 — ORM で DB 操作\n④ Docker 入門 — コンテナでデプロイ'),
        sec('学習の道筋', 'Node.js はツールチェーンもアプリケーションも支える基盤です。小さな CLI スクリプトを書くことから始め、HTTP サーバー、DB 連携へ段階的に進みましょう。\n\nおめでとうございます！Node.js 入門をすべて学びました 🎉', { tip: '自分だけの API サーバーを立ち上げて、フロントエンドから fetch してみてください。' }),
        sec('実践チェックリスト', '• ESM（import/export）を使っているか\n• 環境変数でシークレットを管理しているか\n• async/await で非同期エラーを捕捉しているか\n• ルートをファイル分割して保守性を保っているか'),
      ]),
    ],
  },
  {
    slug: 'npm',
    componentName: 'NpmTutorial',
    courseTitle: 'npm',
    sidebarTitle: 'npm チュートリアル',
    heroAccent: 'npm',
    heroSub: 'Node.js のパッケージマネージャー npm。インストール、スクリプト、バージョン管理を10レッスンで学びます。',
    feature1: '依存関係の解決やセマンティックバージョニングをデモで確認できます。',
    icon: '📦',
    logoIcon: '📦',
    section: 'core',
    subtitle: 'パッケージ管理',
    description: 'package.json、npm install、semver、lockfile、workspaces まで。JavaScript プロジェクトの依存関係管理を10レッスンで学びます。',
    gradient: 'linear-gradient(135deg, #cb3837, #961a1a)',
    lessons: [
      lesson('intro', 'npm とは？', 'npm の役割と Node.js エコシステムでの位置づけを学びます', [
        sec('npm の3つの意味', 'npm は① Node Package Manager（ツール）、② npm レジストリ（パッケージの公開場所）、③ npm 社（運営会社）を指します。開発者が最も触るのは①と②です。\n\n`npm install react` でレジストリからパッケージをダウンロードし、プロジェクトの node_modules に配置します。世界中の開発者が作った数百万のパッケージを再利用できるのが npm の力です。'),
        sec('yarn と pnpm', '代替のパッケージマネージャーに yarn と pnpm があります。pnpm はディスク容量を節約するシンボリックリンク方式、yarn は Plug\'n\'Play など独自機能があります。\n\n基本概念（package.json、semver、lockfile）は共通です。npm を理解すれば他のツールにもすぐ移行できます。', { tip: 'デモで npm install の実行ログを追い、何が起きているか確認してみてください。' }),
        sec('node_modules', 'インストールされたパッケージは node_modules フォルダに置かれます。依存パッケージの依存（推移的依存）もすべてここに展開されるため、サイズが大きくなりがちです。\n\nnode_modules は Git にコミットしません。.gitignore に追加し、lockfile から `npm ci` で再現します。'),
      ]),
      lesson('package-json', 'package.json', 'プロジェクトの設計図となる package.json を学びます', [
        sec('主要フィールド', '• name — パッケージ名（公開時は一意）\n• version — セマンティックバージョン\n• description — 説明文\n• main / exports — エントリポイント\n• scripts — 実行コマンド\n• dependencies — 本番依存\n• devDependencies — 開発時のみの依存', { code: '{\n  "name": "my-app",\n  "version": "1.0.0",\n  "type": "module",\n  "scripts": { "dev": "vite", "build": "vite build" },\n  "dependencies": { "react": "^18.3.0" },\n  "devDependencies": { "vite": "^5.0.0" }\n}' }),
        sec('dependencies と devDependencies', 'アプリの実行に必要なものは dependencies、ビルド・テスト・Lint だけに使うものは devDependencies に入れます。`npm install -D eslint` で devDependencies に追加します。\n\n本番デプロイ時は `npm ci --omit=dev` で devDependencies を除外し、イメージサイズとインストール時間を削減します。'),
        sec('exports フィールド', 'Node.js の "exports" フィールドは、パッケージの公開 API を明示します。サブパス import を制御でき、内部ファイルへの直接アクセスを防げます。\n\nライブラリを公開する際は exports を設計し、使う側が安定したインポートパスを使えるようにします。'),
      ]),
      lesson('install', 'パッケージのインストール', 'npm install の挙動とオプションを学びます', [
        sec('基本コマンド', '`npm install` は package.json の依存をすべてインストールします。`npm install lodash` は dependencies に追加してインストール、`npm install -D typescript` は devDependencies に追加します。\n\n`npm uninstall lodash` で削除、`npm update` で semver 範囲内の最新版に更新します。', { code: 'npm install          # package.json の全依存\nnpm install axios    # 追加インストール\nnpm install -D vitest  # 開発依存として追加\nnpm uninstall axios  # 削除' }),
        sec('グローバルインストール', '`npm install -g typescript` でグローバルにインストールします。CLI ツール（create-vite、eslint など）向きですが、プロジェクト依存はローカルが推奨です。\n\nnpx（または npm exec）で一時実行すれば、グローバルインストール不要で `npx create-vite@latest` のように使えます。'),
        sec('インストールの仕組み', 'npm は依存グラフを解決し、互換性のあるバージョンを選んでダウンロードします。peerDependencies（React プラグインが React 本体を要求するなど）の警告は無視せず、適切なバージョンを揃えます。\n\n`npm ls react` で依存ツリーを確認、`npm why lodash` でなぜそのパッケージが入っているか調べられます。', { tip: 'デモで npm ls の出力を見て、推移的依存の深さを確認してみてください。' }),
      ]),
      lesson('semver', 'セマンティックバージョニング', 'バージョン番号の意味と ^ ~ の使い方を学びます', [
        sec('SemVer の形式', 'バージョンは MAJOR.MINOR.PATCH（例: 2.4.1）です。\n\n• MAJOR — 破壊的変更（API の非互換）\n• MINOR — 後方互換の機能追加\n• PATCH — 後方互換のバグ修正\n\n1.0.0 未満は「初期開発」で破壊的変更が MINOR でも起きることがあります。'),
        sec('バージョン範囲', '• `^18.3.0` — 18.x.x の最新（18.4.0 OK、19.0.0 NG）\n• `~18.3.0` — 18.3.x の最新（18.3.5 OK、18.4.0 NG）\n• `18.3.0` — ピン留め（厳密にこのバージョン）\n\n^ が最も一般的です。ライブラリは ^、アプリの本番依存は lockfile で固定が安全です。', { code: '"dependencies": {\n  "react": "^18.3.1",    // 18.x の最新\n  "lodash": "~4.17.21",  // 4.17.x の最新\n  "express": "4.21.0"    // 固定\n}' }),
        sec('アップデート戦略', '定期的に `npm outdated` で古いパッケージを確認し、セキュリティパッチは早めに適用します。MAJOR アップデートは CHANGELOG を読み、破壊的変更への対応を計画します。\n\nRenovate や Dependabot で PR を自動作成するのが大規模プロジェクトでは標準的です。'),
      ]),
      lesson('scripts', 'npm scripts', 'package.json の scripts でタスクを自動化します', [
        sec('scripts の基本', 'scripts はシェルコマンドのエイリアスです。`npm run build` で `"build": "vite build"` を実行します。`prebuild` や `postbuild` で前後処理を自動実行できます。\n\nチーム全員が同じコマンドを使うことで、「私の環境では動く」問題を減らせます。', { code: '"scripts": {\n  "dev": "vite",\n  "build": "tsc && vite build",\n  "preview": "vite preview",\n  "lint": "eslint src/",\n  "test": "vitest run"\n}' }),
        sec('ライフサイクルスクリプト', 'npm install 時に `postinstall` が走り、ネイティブモジュールのビルドなどが行われます。`prepare` は git clone 後や npm pack 前に実行されます。\n\nhusky のセットアップで prepare を使うパターンがよく見られます。意図しないスクリプト実行に注意し、信頼できるパッケージだけを入れましょう。'),
        sec('環境変数の渡し方', 'クロスプラットフォームで環境変数を渡すには cross-env を使います。`"build:staging": "cross-env NODE_ENV=staging vite build"`\n\n.env ファイルは dotenv やフレームワーク（Vite の import.meta.env）が読み込みます。シークレットは scripts に直書きしないでください。'),
      ]),
      lesson('npx', 'npx と npm exec', 'パッケージをインストールせず一時実行する方法を学びます', [
        sec('npx の役割', 'npx はパッケージを一時ダウンロードして実行し、終わったら削除します。`npx create-vite@latest` でプロジェクト生成ツールを毎回最新版で実行できます。\n\nグローバルに汚さず、バージョンも `@latest` や `@5` で指定できるのが利点です。'),
        sec('npm exec', 'npm 7+ では `npm exec` が npx の正式な代替です。`npm exec eslint --fix .` のように動作はほぼ同じです。\n\nローカルにインストール済みのパッケージは node_modules/.bin から実行され、未インストールならレジストリから取得します。', { code: 'npx create-vite@latest my-app -- --template react-ts\nnpx prisma init\nnpm exec tsc --noEmit' }),
        sec('プロジェクト内の CLI', 'devDependencies に入れたツールは `npm run lint` 経由か `npx eslint` で実行します。CI では `npm ci` 後に `npm run test` が定番フローです。\n\npackage.json の scripts にラップすることで、長いオプション付きコマンドを短い名前で呼べます。'),
      ]),
      lesson('lockfile', 'package-lock.json', 'ロックファイルの役割と npm ci を学びます', [
        sec('lockfile の目的', 'package-lock.json はインストール時に解決された exact バージョンを記録します。チームメンバー・CI・本番で同じ依存ツリーを再現するために必須です。\n\npackage.json の ^18.3.0 は範囲ですが、lockfile は 18.3.1 のような具体バージョンを固定します。'),
        sec('npm ci vs npm install', 'CI や本番では `npm ci`（clean install）を使います。lockfile を厳密に守り、node_modules を削除してから再インストールするため、再現性が高いです。\n\n開発時の新規追加は `npm install`、lockfile 更新後の同期は `npm ci` と使い分けます。', { code: '# CI（GitHub Actions）の例\n- run: npm ci\n- run: npm run build\n- run: npm test' }),
        sec('lockfile の管理', 'package-lock.json は Git にコミットします。手動編集は避け、npm コマンドで更新します。コンフリクト時は `npm install` で再生成するか、マージ後に `rm -rf node_modules && npm ci` で確認します。\n\npnpm は pnpm-lock.yaml、yarn は yarn.lock が同じ役割です。'),
      ]),
      lesson('publish', 'パッケージの公開', 'npm レジストリへの公開手順を学びます', [
        sec('公開の準備', 'package.json の name はレジストリで一意である必要があります。スコープ付き `@myorg/utils` は組織向きです。files フィールドで公開するファイルを限定し、src やテストを除外します。\n\n`npm login` で認証し、`npm publish` で公開します。初回は `npm publish --access public`（スコープパッケージの場合）が必要です。', { code: '{\n  "name": "@myorg/string-utils",\n  "version": "1.0.0",\n  "files": ["dist/", "README.md"],\n  "main": "dist/index.js",\n  "types": "dist/index.d.ts"\n}' }),
        sec('バージョンの更新', '`npm version patch` で PATCH を上げて git tag を作成します。minor、major も同様です。公開前に `npm pack` で tarball の内容を確認できます。\n\n公開後のバージョンは取り消せません（unpublish は24時間以内など制限あり）。慎重にバージョンを上げましょう。'),
        sec('プライベートパッケージ', '組織向け npm の有料プランや GitHub Packages、Verdaccio（自前レジストリ）でプライベートパッケージを運用できます。\n\n社内ライブラリを npm 化すると、複数プロジェクトでの再利用が楽になります。'),
      ]),
      lesson('workspaces', 'npm workspaces', 'モノレポで複数パッケージを管理する方法を学びます', [
        sec('workspaces の設定', 'package.json に `"workspaces": ["packages/*"]` を追加すると、サブパッケージをまとめて管理できます。ルートで `npm install` すると全ワークスペースの依存が hoist されます。\n\nTurborepo や Nx と組み合わせてビルド・テストを高速化するのが現代的なモノレポ構成です。', { code: '{\n  "name": "my-monorepo",\n  "workspaces": ["apps/*", "packages/*"]\n}\n\n# 特定ワークスペースに追加\nnpm install react -w apps/web' }),
        sec('ワークスペース間の依存', '同じモノレポ内のパッケージは `"@myorg/ui": "*"` や `"workspace:*"` で参照します。npm link の手動操作が不要になります。\n\n共通 UI コンポーネント、共有 utils、設定パッケージ（eslint-config）をワークスペースに分けるパターンが一般的です。'),
        sec('モノレポの注意点', '依存の hoist により、サブパッケージが宣言していないパッケージが使える「幽霊依存」が起きることがあります。pnpm は厳格な依存管理でこれを防ぎます。\n\nルートの package.json に devDependencies を集約し、各パッケージは必要最小限に保つのがベストプラクティスです。', { tip: 'Turborepo チュートリアルでモノレポのビルド最適化も学んでみてください。' }),
      ]),
      lesson('next-steps', '次のステップ', 'npm の学習を続けるための道筋を確認します', [
        sec('次に学ぶこと', '① Node.js 入門 — ランタイムの基礎\n② Turborepo — モノレポのビルド高速化\n③ CI/CD — npm ci をパイプラインに組み込む\n④ セキュリティ — npm audit と依存の脆弱性'),
        sec('学習の道筋', 'npm は毎日の開発で触るツールです。lockfile をコミットする、npm ci を CI で使う、semver を理解する——この3つだけでもトラブルが大幅に減ります。\n\nおめでとうございます！npm 入門をすべて学びました 🎉', { tip: '自分のプロジェクトの package.json を見直し、不要な依存を整理してみてください。' }),
        sec('実践チェックリスト', '• lockfile を Git 管理しているか\n• devDependencies と dependencies を正しく分けているか\n• npm audit で脆弱性を定期チェックしているか\n• scripts でチーム共通のコマンドを定義しているか'),
      ]),
    ],
  },
  {
    slug: 'auth',
    componentName: 'AuthTutorial',
    courseTitle: '認証',
    sidebarTitle: '認証 チュートリアル',
    heroAccent: 'Authentication',
    heroSub: 'パスワードハッシュ、セッション、JWT、OAuth まで。Web アプリの認証を10レッスンで体系的に学びます。',
    feature1: 'ログインフローとトークン検証の流れをデモで追跡できます。',
    icon: '🔐',
    logoIcon: '🔐',
    section: 'framework',
    subtitle: 'ログインとセッション管理',
    description: '認証の基本概念、パスワードハッシュ、Cookie セッション、JWT、OAuth 2.0、ミドルウェアまで。安全な認証実装を10レッスンで学びます。',
    gradient: 'linear-gradient(135deg, #f59e0b, #ef4444)',
    lessons: [
      lesson('intro', '認証とは？', '認証と認可の違い、Web アプリでの役割を理解します', [
        sec('認証の基本', '認証（Authentication）は「この人は誰か」を確認する仕組みです。メールとパスワード、OAuth、生体認証などが手段になります。認可（Authorization）は「この人は何をしてよいか」を決める別のレイヤーです。\n\nログイン画面でパスワードを入力するのは認証、管理者だけが設定画面に入れるのは認可です。混同すると設計ミスが起きやすいので、最初に区別を覚えましょう。'),
        sec('ステートレス vs ステートフル', 'HTTP はステートレスですが、ログイン状態を維持するにはサーバー側セッション（ステートフル）か JWT トークン（ステートレス寄り）が必要です。どちらも「本人確認済み」という証明をリクエスト間で引き継ぎます。\n\n小規模アプリは JWT、従来型の Web アプリは Cookie セッションが多いですが、SPA + API では JWT やセッション Cookie のどちらも使われます。', { tip: 'デモでログイン前後のリクエストヘッダーの違いを確認してみてください。' }),
        sec('認証の全体像', '典型的なフローは①ユーザーが認証情報を送信 → ②サーバーが検証 → ③セッション ID や JWT を発行 → ④以降のリクエストでその証明を提示 → ⑤サーバーが有効性を確認します。\n\nフロントエンドは「トークンを安全に保持し、API に付与する」、バックエンドは「検証と権限チェック」を担当します。'),
      ]),
      lesson('passwords', 'パスワードの安全な扱い', 'ハッシュ化、ソルト、bcrypt の実践を学びます', [
        sec('平文保存は絶対禁止', 'パスワードを DB に平文で保存すると、漏洩時に全ユーザーのアカウントが危険にさらされます。必ず一方向ハッシュ関数で変換し、元のパスワードは復元できない形で保存します。\n\nbcrypt、scrypt、Argon2 が推奨されます。Node.js では bcrypt や argon2 パッケージが広く使われています。', { code: 'import bcrypt from "bcrypt";\n\nconst hash = await bcrypt.hash(password, 12);\nconst isValid = await bcrypt.compare(inputPassword, hash);' }),
        sec('ソルトの役割', 'ソルトはハッシュ前に付加するランダム文字列です。同じパスワードでもユーザーごとに異なるハッシュになり、レインボーテーブル攻撃を防ぎます。bcrypt はソルトをハッシュ文字列に含めるため、別カラム管理が不要です。\n\nコストファクター（bcrypt の rounds）を上げると計算時間が増え、ブルートフォース攻撃に強くなりますが、ログイン処理も遅くなります。12 前後が一般的なバランスです。'),
        sec('パスワードポリシー', '最低8文字以上、大小英数字の混在を推奨するサイトは多いですが、NIST の最新ガイドラインは「長さを優先し、複雑さの強制は控えめに」としています。\n\nパスワードリセットはワンタイムトークン（有効期限付き）で行い、メールにパスワードそのものを送ることはありません。', { tip: 'デモで同じパスワードでもハッシュ値が毎回異なることを確認してみてください。' }),
      ]),
      lesson('sessions', 'セッションと Cookie', 'サーバー側セッション管理の仕組みを学びます', [
        sec('セッションの流れ', 'ログイン成功時、サーバーはセッション ID を生成してサーバー側ストア（メモリ、Redis など）にユーザー情報を保存します。クライアントには Set-Cookie でセッション ID だけを返します。\n\n以降のリクエストで Cookie が自動送信され、サーバーはセッション ID でユーザーを特定します。ログアウトはセッションをサーバー側で削除します。'),
        sec('Cookie のセキュリティ属性', 'HttpOnly — JavaScript から Cookie にアクセス不可（XSS 対策）。Secure — HTTPS 通信のみ送信。SameSite — クロスサイトリクエストでの送信を制限（CSRF 対策）。\n\n本番環境では3つすべてを適切に設定するのが基本です。開発環境の localhost では Secure が効かない場合があるため注意します。', { code: 'res.cookie("sessionId", id, {\n  httpOnly: true,\n  secure: process.env.NODE_ENV === "production",\n  sameSite: "lax",\n  maxAge: 7 * 24 * 60 * 60 * 1000,\n});' }),
        sec('セッションストアの選択', '開発時はメモリストアで十分ですが、本番では Redis が定番です。複数サーバーでセッションを共有でき、再起動でもセッションが失われません。\n\nセッションの有効期限（TTL）を設定し、一定時間操作がなければ自動ログアウトさせるのが一般的です。'),
      ]),
      lesson('jwt', 'JWT（JSON Web Token）', 'JWT の構造と署名検証を学びます', [
        sec('JWT の3部構成', 'JWT は Header.Payload.Signature の3部分を Base64URL エンコードして `.` で結合した文字列です。Payload にユーザー ID やロールを含めますが、暗号化されていないため機密情報は入れません。\n\nSignature はサーバーの秘密鍵（またはシークレット）で署名され、改ざんを検出できます。', { code: 'import jwt from "jsonwebtoken";\n\nconst token = jwt.sign(\n  { userId: 42, role: "admin" },\n  process.env.JWT_SECRET,\n  { expiresIn: "1h" }\n);\nconst payload = jwt.verify(token, process.env.JWT_SECRET);' }),
        sec('アクセストークンとリフレッシュトークン', 'アクセストークンは短命（15分〜1時間）で API 認証に使います。リフレッシュトークンは長命で、アクセストークン期限切れ時に新しいものを発行します。\n\nリフレッシュトークンは HttpOnly Cookie に保存し、アクセストークンはメモリ（変数）に保持するパターンが XSS に強い設計です。localStorage に JWT を置くのはリスクが高いとされています。'),
        sec('JWT の注意点', 'JWT は「ログアウト」が難しい——トークンが有効期限内ならサーバーは受け付けます。ブラックリストや短命トークン + リフレッシュで対処します。\n\n秘密鍵の漏洩は全トークンが危険になるため、環境変数で管理し、定期的にローテーションを検討してください。', { tip: 'jwt.io でトークンをデコードし、Payload の内容を確認してみてください。' }),
      ]),
      lesson('oauth', 'OAuth 2.0 とソーシャルログイン', 'Google・GitHub ログインの仕組みを学びます', [
        sec('OAuth の登場人物', 'リソースオーナー（ユーザー）、クライアント（あなたのアプリ）、認可サーバー（Google 等）、リソースサーバー（API）の4者が関わります。ユーザーはパスワードをクライアントに渡さず、認可サーバー経由でアクセス権を委譲します。\n\n「Google でログイン」は OAuth 2.0 の Authorization Code フローが最も一般的です。'),
        sec('Authorization Code フロー', '①アプリが Google の認可画面にリダイレクト → ②ユーザーが許可 → ③認可コードがコールバック URL に返る → ④サーバーがコードをアクセストークンに交換 → ⑤トークンでユーザー情報を取得。\n\n認可コードの交換はサーバー側で行います（client_secret が必要）。フロントエンドだけで完結させないのが鉄則です。', { code: 'const { tokens } = await oauth2Client.getToken(code);\nconst userInfo = await fetch("https://www.googleapis.com/oauth2/v2/userinfo", {\n  headers: { Authorization: `Bearer ${tokens.access_token}` },\n});' }),
        sec('実装ライブラリ', 'Next.js では Auth.js（旧 NextAuth.js）、Passport.js（Express）、Supabase Auth などが定番です。自前で OAuth フローを実装するより、実績のあるライブラリを使う方が安全です。\n\nリダイレクト URI の登録ミスは OAuth エラーの最多原因です。開発用と本番用を正確に登録しましょう。'),
      ]),
      lesson('middleware', '認証ミドルウェア', 'API ルートを保護するミドルウェアを学びます', [
        sec('ミドルウェアの役割', '認証ミドルウェアはリクエストのたびにトークンやセッションを検証し、有効なら req.user にユーザー情報を付与して次のハンドラへ渡します。無効なら 401 を返します。\n\nルートごとに認証チェックを書くのではなく、ミドルウェアに集約するのが保守性の基本です。'),
        sec('Express の実装例', 'Bearer トークンを Authorization ヘッダーから取り出し、JWT を検証します。エラーは統一フォーマットで返すとフロントエンドの処理が楽になります。', { code: 'function authMiddleware(req, res, next) {\n  const token = req.headers.authorization?.split(" ")[1];\n  if (!token) return res.status(401).json({ error: "認証が必要です" });\n  try {\n    req.user = jwt.verify(token, process.env.JWT_SECRET);\n    next();\n  } catch {\n    res.status(401).json({ error: "トークンが無効です" });\n  }\n}' }),
        sec('Next.js の middleware', 'Next.js 13+ では middleware.ts でルート単位の認証が可能です。Cookie のセッションを検証し、未認証なら /login にリダイレクトします。\n\nEdge Runtime で動くため、DB 直接アクセスはできません。JWT 検証やセッション Cookie の存在チェックが向いています。', { tip: '公開 API と保護 API をルート設計で明確に分けてください。' }),
      ]),
      lesson('frontend-auth', 'フロントエンドの認証 UI', 'ログインフォームと認証状態管理を学びます', [
        sec('認証状態の管理', 'React では Context や Zustand で user 状態をグローバル管理します。アプリ起動時に /api/me でセッション有効性を確認し、有効ならユーザー情報をセットします。\n\nローディング状態（checking auth）を用意し、確認中に保護ページを一瞬見せないのが UX の基本です。'),
        sec('ログインフォームの実装', 'フォーム送信 → API 呼び出し → 成功時にユーザー状態を更新 → リダイレクト。エラー時はフィールド単位またはフォーム全体のエラーメッセージを表示します。', { code: 'const res = await fetch("/api/login", {\n  method: "POST",\n  headers: { "Content-Type": "application/json" },\n  body: JSON.stringify({ email, password }),\n  credentials: "include",\n});\nif (!res.ok) { setError("ログインに失敗しました"); return; }' }),
        sec('Protected Route パターン', '未認証ユーザーをログインページへリダイレクトするラッパーコンポーネントを作ります。if (!user) return <Navigate to="/login" /> のようなパターンです。\n\nロールベースの制御（admin 専用ページなど）は user.role をチェックし、403 ページを表示します。'),
      ]),
      lesson('security-practices', '認証のセキュリティ', 'レート制限、CSRF、多要素認証を学びます', [
        sec('ブルートフォース対策', 'ログイン試行にレート制限を設けます。同一 IP や同一メールアドレスから短時間に多数の失敗があったら一時ブロックします。express-rate-limit や Redis ベースのカウンターが使われます。\n\nアカウントロックアウトはユーザー体験とのトレードオフです。CAPTCHA を段階的に表示する手法も有効です。'),
        sec('CSRF 対策', 'Cookie ベースセッションでは CSRF トークンが必須です。フォームに hidden フィールドでトークンを埋め込み、サーバーが照合します。SameSite=Lax/Strict も有効な防御層です。\n\nJWT を Authorization ヘッダーで送る方式は CSRF の影響を受けにくいですが、XSS には依然として脆弱です。'),
        sec('多要素認証（MFA）', 'TOTP（Google Authenticator 等）や WebAuthn（パスキー）で第二要素を追加します。パスワード漏洩だけではアカウントを守れます。\n\n重要なサービスでは MFA を推奨または必須にするのが標準になりつつあります。', { code: 'import { authenticator } from "otplib";\nconst secret = authenticator.generateSecret();\nconst isValid = authenticator.verify({ token: userInput, secret });' }),
      ]),
      lesson('implementation', '認証の実装パターン', '実務で使われる認証アーキテクチャを学びます', [
        sec('BFF パターン', 'Backend for Frontend はフロント専用の API 層を置き、認証・トークン管理をそこに集約します。マイクロサービスへのトークン伝播も BFF が担当します。\n\nNext.js の API Routes や Server Actions が BFF として機能するケースが増えています。'),
        sec('Auth.js の概要', 'Auth.js は Next.js 向けの認証ライブラリです。OAuth プロバイダー、Credentials、セッション管理を設定ファイルで宣言的に定義できます。', { code: 'import NextAuth from "next-auth";\nimport Google from "next-auth/providers/google";\n\nexport const { handlers, auth } = NextAuth({\n  providers: [Google],\n});' }),
        sec('テストとデバッグ', '認証フローは E2E テスト（Playwright）でカバーします。テスト用ユーザーを DB シードし、ログイン → 保護ページアクセス → ログアウトのシナリオを自動化します。\n\n401/403 のレスポンス形式を統一すると、フロントエンドのエラーハンドリングがシンプルになります。'),
      ]),
      lesson('next-steps', '次のステップ', '認証の学習を続けるための道筋を確認します', [
        sec('次に学ぶこと', '① Webセキュリティ — XSS・CSRF の深掘り\n② Supabase 入門 — 認証込み BaaS\n③ OAuth 2.0 仕様書 — フローの詳細\n④ WebAuthn — パスキー認証'),
        sec('学習の道筋', '認証はセキュリティと直結する最重要テーマです。パスワードをハッシュ化する、Cookie に HttpOnly を付ける、JWT を localStorage に置かない——基本を守るだけで多くの事故を防げます。\n\nおめでとうございます！認証入門をすべて学びました 🎉', { tip: '自分のアプリにログイン機能を実装し、ログアウト後に保護 API が 401 を返すことを確認してみてください。' }),
        sec('実践チェックリスト', '• パスワードは bcrypt/Argon2 でハッシュ化しているか\n• Cookie に HttpOnly・Secure・SameSite を設定しているか\n• JWT の秘密鍵を環境変数で管理しているか\n• ログインにレート制限を設けているか\n• OAuth の client_secret をフロントに露出していないか'),
      ]),
    ],
  },
  {
    slug: 'prisma',
    componentName: 'PrismaTutorial',
    courseTitle: 'Prisma',
    sidebarTitle: 'Prisma チュートリアル',
    heroAccent: 'Prisma ORM',
    heroSub: 'スキーマ定義からマイグレーション、リレーション、トランザクションまで。型安全な DB 操作を10レッスンで学びます。',
    feature1: 'スキーマ変更とクエリ結果をデモでリアルタイム確認できます。',
    icon: '🗄️',
    logoIcon: '🗄️',
    section: 'framework',
    subtitle: '型安全な ORM',
    description: 'Prisma のセットアップ、スキーマ設計、CRUD、リレーション、マイグレーション、高度なクエリまで。Node.js の DB 操作を10レッスンで学びます。',
    gradient: 'linear-gradient(135deg, #2d3748, #4a5568)',
    lessons: [
      lesson('intro', 'Prisma とは？', 'ORM の概念と Prisma の3つのコンポーネントを理解します', [
        sec('ORM とは', 'ORM（Object-Relational Mapping）は、オブジェクト指向のコードでリレーショナル DB を操作する仕組みです。SQL を直接書く代わりに、型安全な API でデータを扱えます。\n\nPrisma は Node.js/TypeScript エコシステムで最も人気の ORM です。自動生成される型により、コンパイル時にクエリのミスを検出できます。'),
        sec('Prisma の3コンポーネント', 'Prisma Schema — DB 構造を宣言的に定義。Prisma Client — 自動生成される型安全なクエリ API。Prisma Migrate — スキーマ変更を SQL マイグレーションとして管理。\n\nこの3つが連携し、「スキーマを書く → マイグレーション → 型安全にクエリ」という開発フローを実現します。', { tip: 'デモで schema.prisma のモデル定義と生成される TypeScript 型の対応を確認してみてください。' }),
        sec('対応データベース', 'PostgreSQL、MySQL、SQLite、SQL Server、MongoDB などに対応しています。開発時は SQLite、本番は PostgreSQL という切り替えも datasource の URL 変更だけで可能です。'),
      ]),
      lesson('setup', 'セットアップ', 'Prisma のインストールと初期化を学びます', [
        sec('インストール', '`npm install prisma --save-dev` と `npm install @prisma/client` で導入します。`npx prisma init` で schema.prisma と .env が生成されます。', { code: 'npm install prisma --save-dev\nnpm install @prisma/client\nnpx prisma init' }),
        sec('接続設定', '.env に DATABASE_URL を設定し、schema.prisma の datasource で参照します。PostgreSQL の例: postgresql://user:pass@localhost:5432/mydb', { code: 'datasource db {\n  provider = "postgresql"\n  url      = env("DATABASE_URL")\n}\n\ngenerator client {\n  provider = "prisma-client-js"\n}' }),
        sec('クライアント生成', 'スキーマを変更したら `npx prisma generate` で Prisma Client を再生成します。CI では postinstall スクリプトに含めるのが一般的です。\n\n`npx prisma studio` で GUI の DB ブラウザが使え、開発中のデータ確認に便利です。'),
      ]),
      lesson('schema', 'スキーマ設計', 'モデル、フィールド型、制約の定義を学びます', [
        sec('モデルの基本', 'model はテーブルに対応します。フィールドはカラムで、@id は主キー、@default はデフォルト値、@unique は一意制約です。', { code: 'model User {\n  id        Int      @id @default(autoincrement())\n  email     String   @unique\n  name      String?\n  createdAt DateTime @default(now())\n  posts     Post[]\n}' }),
        sec('フィールド型', 'String、Int、Float、Boolean、DateTime、Json などが使えます。? を付けるとオプショナル（NULL 許可）になります。\n\n@default(now()) は作成日時、@updatedAt は更新時に自動セットされるタイムスタンプです。'),
        sec('命名規則', 'モデル名は PascalCase、フィールド名は camelCase が Prisma の慣習です。DB のテーブル名は @@map("users") でスネークケースにマッピングできます。\n\n既存 DB からスキーマを生成するには `npx prisma db pull`（introspection）を使います。', { tip: 'スキーマを先に設計し、マイグレーションで DB に反映する「スキーマファースト」が Prisma の基本フローです。' }),
      ]),
      lesson('crud', 'CRUD 操作', '作成・読取・更新・削除の基本クエリを学びます', [
        sec('Create', 'prisma.model.create() で1件作成、createMany() で複数作成します。戻り値は作成されたレコードのオブジェクトです。', { code: 'const user = await prisma.user.create({\n  data: { email: "taro@example.com", name: "太郎" },\n});' }),
        sec('Read', 'findUnique() は一意フィールドで1件取得、findMany() は条件に合う複数件、findFirst() は最初の1件です。存在しない場合は null が返ります。', { code: 'const user = await prisma.user.findUnique({ where: { email: "taro@example.com" } });\nconst admins = await prisma.user.findMany({\n  where: { role: "admin" },\n  orderBy: { createdAt: "desc" },\n  take: 10,\n});' }),
        sec('Update と Delete', 'update() は1件更新、updateMany() は条件に合う複数件を更新。delete() と deleteMany() も同様です。', { code: 'await prisma.user.update({ where: { id: 1 }, data: { name: "花子" } });\nawait prisma.user.delete({ where: { id: 1 } });' }),
      ]),
      lesson('relations', 'リレーション', '1対多、多対多のモデル関係を学びます', [
        sec('1対多（One-to-Many）', 'Post に author User への参照を持たせます。User 側には posts Post[] の逆参照を定義します。', { code: 'model Post {\n  id       Int  @id @default(autoincrement())\n  title    String\n  author   User @relation(fields: [authorId], references: [id])\n  authorId Int\n}' }),
        sec('リレーションを含むクエリ', 'include で関連データを一緒に取得します。N+1 問題を防ぐため、必要なリレーションは1クエリで include しましょう。', { code: 'const userWithPosts = await prisma.user.findUnique({\n  where: { id: 1 },\n  include: { posts: true },\n});' }),
        sec('多対多（Many-to-Many）', '暗黙の多対多は Prisma が中間テーブルを自動生成します。明示的な多対多は中間モデルを自分で定義し、追加フィールド（createdAt など）を持たせられます。\n\nカスケード削除は onDelete: Cascade で親削除時に子も削除されます。', { tip: 'include のネストは深くしすぎないでください。必要なフィールドだけ select で絞るのがパフォーマンスの基本です。' }),
      ]),
      lesson('migrations', 'マイグレーション', 'スキーマ変更のバージョン管理を学びます', [
        sec('開発フロー', 'schema.prisma を編集 → `npx prisma migrate dev --name add_user_role` でマイグレーション SQL を生成・適用 → Prisma Client が自動再生成されます。\n\nマイグレーションファイルは prisma/migrations/ に保存され、Git で管理します。'),
        sec('本番デプロイ', '本番では `npx prisma migrate deploy` で未適用のマイグレーションを順に実行します。CI/CD パイプラインに組み込むのが定番です。\n\n`prisma db push` はマイグレーション履歴なしでスキーマを直接反映する開発用コマンドです。本番では使いません。', { code: 'npx prisma migrate dev --name add_comments\nnpx prisma migrate deploy' }),
        sec('マイグレーションのベストプラクティス', '破壊的変更（カラム削除、型変更）は段階的に行います。まず新カラム追加 → データ移行 → 旧カラム削除の3ステップが安全です。\n\nマイグレーションのコンフリクトはチーム開発で起きがちです。早めにマージし、ローカルで解決してから push しましょう。'),
      ]),
      lesson('queries', '高度なクエリ', 'フィルタ、ページネーション、集計を学びます', [
        sec('フィルタリング', 'where に複合条件を指定できます。AND、OR、NOT、contains、startsWith、gt/gte/lt/lte など豊富なオペレータがあります。', { code: 'const results = await prisma.post.findMany({\n  where: {\n    AND: [\n      { published: true },\n      { title: { contains: "Prisma", mode: "insensitive" } },\n    ],\n  },\n});' }),
        sec('ページネーション', 'take（取得件数）と skip（オフセット）でページネーションします。cursor ベースは cursor と skip: 1 を組み合わせます。', { code: 'const page = await prisma.post.findMany({\n  take: 20,\n  skip: (pageNum - 1) * 20,\n  orderBy: { createdAt: "desc" },\n});' }),
        sec('集計', 'count、aggregate（sum、avg、min、max）、groupBy でデータ集計ができます。レポート機能やダッシュボードで活きます。', { code: 'const stats = await prisma.order.aggregate({\n  _sum: { amount: true },\n  _avg: { amount: true },\n  _count: true,\n});' }),
      ]),
      lesson('transactions', 'トランザクション', '複数操作の原子性を保証する方法を学びます', [
        sec('interactive transactions', '$transaction に async 関数を渡し、複数の操作を1つのトランザクションで実行します。途中でエラーが起きればすべてロールバックされます。', { code: 'await prisma.$transaction(async (tx) => {\n  const order = await tx.order.create({ data: { userId: 1, total: 5000 } });\n  await tx.product.update({\n    where: { id: productId },\n    data: { stock: { decrement: 1 } },\n  });\n});' }),
        sec('バッチトランザクション', '配列にクエリを並べて一括実行するシンプルな形式もあります。すべて成功かすべて失敗のどちらかです。', { code: 'await prisma.$transaction([\n  prisma.account.update({ where: { id: 1 }, data: { balance: { decrement: 100 } } }),\n  prisma.account.update({ where: { id: 2 }, data: { balance: { increment: 100 } } }),\n]);' }),
        sec('分離レベル', 'デフォルトの分離レベルは DB に依存します。高い分離レベルは整合性が強いがデッドロックのリスクも上がります。決済や在庫管理ではトランザクションの理解が必須です。', { tip: 'デモでトランザクション途中のエラー時にデータがロールバックされることを確認してみてください。' }),
      ]),
      lesson('advanced', '応用テクニック', 'ミドルウェア、生 SQL、シーディングを学びます', [
        sec('Prisma Middleware', '$use でクエリ前後に処理を挟めます。ログ出力、ソフトデリート、マルチテナントのフィルタリングなどに使います。', { code: 'prisma.$use(async (params, next) => {\n  const before = Date.now();\n  const result = await next(params);\n  console.log(`Query took ${Date.now() - before}ms`);\n  return result;\n});' }),
        sec('生 SQL', '$queryRaw と $executeRaw で Prisma がカバーしない複雑なクエリを実行できます。SQL インジェクション防止のため、必ずプレースホルダーを使います。', { code: 'const result = await prisma.$queryRaw`\n  SELECT u.name, COUNT(p.id) as post_count\n  FROM "User" u LEFT JOIN "Post" p ON u.id = p."authorId"\n  GROUP BY u.id\n`;' }),
        sec('シーディング', 'prisma/seed.ts で初期データを投入します。package.json に prisma.seed を設定し、`npx prisma db seed` で実行します。\n\n開発環境の再現性とテストデータの準備に不可欠です。'),
      ]),
      lesson('next-steps', '次のステップ', 'Prisma の学習を続けるための道筋を確認します', [
        sec('次に学ぶこと', '① Supabase 入門 — Prisma なしの DB + 認証\n② Docker 入門 — DB コンテナの運用\n③ GraphQL 入門 — Prisma + GraphQL API\n④ 認証入門 — ユーザー管理との連携'),
        sec('学習の道筋', 'Prisma はスキーマ駆動開発の強力なツールです。型安全なクエリ、マイグレーション管理、Prisma Studio——これらを使いこなせば DB 操作の生産性が大幅に上がります。\n\nおめでとうございます！Prisma 入門をすべて学びました 🎉', { tip: '小さなブログ API を Prisma で作り、CRUD + リレーションを一通り実装してみてください。' }),
        sec('実践チェックリスト', '• schema.prisma を Git 管理しているか\n• マイグレーションファイルをコミットしているか\n• N+1 を include で回避しているか\n• トランザクションで整合性を保っているか\n• 本番で migrate deploy を CI に組み込んでいるか'),
      ]),
    ],
  },
  {
    slug: 'graphql',
    componentName: 'GraphqlTutorial',
    courseTitle: 'GraphQL',
    sidebarTitle: 'GraphQL チュートリアル',
    heroAccent: 'GraphQL API',
    heroSub: 'スキーマ定義、クエリ、ミューテーション、Apollo Client まで。必要なデータだけを取得する API を10レッスンで学びます。',
    feature1: 'クエリの構造とレスポンスの対応をデモで確認できます。',
    icon: '◈',
    logoIcon: '◈',
    section: 'framework',
    subtitle: '宣言的な API クエリ',
    description: 'GraphQL の基本概念、スキーマ設計、クエリ・ミューテーション、リゾルバ、Apollo Client まで。REST の代替アプローチを10レッスンで学びます。',
    gradient: 'linear-gradient(135deg, #e535ab, #8b2fc9)',
    lessons: [
      lesson('intro', 'GraphQL とは？', 'REST との違いと GraphQL の利点を理解します', [
        sec('REST の課題', 'REST API ではエンドポイントごとに固定のレスポンス構造があります。一覧画面で名前だけ欲しいのに全フィールドが返る、詳細画面で関連データが別 API になる——オーバーフェッチとアンダーフェッチが起きます。\n\nGraphQL はクライアントが「必要なフィールドだけ」を指定して1回のリクエストで取得できるクエリ言語です。'),
        sec('GraphQL の特徴', '単一エンドポイント（通常 /graphql）に POST でクエリを送ります。型システム（スキーマ）で API の契約が明確になり、Introspection でドキュメント自動生成も可能です。\n\nFacebook が2012年に開発し、2015年に公開。GitHub、Shopify、Twitter など大規模サービスで採用されています。', { tip: 'デモで同じエンドポイントに異なるクエリを送り、レスポンスの差を確認してみてください。' }),
        sec('REST との使い分け', 'GraphQL が向くのは複雑なデータ要件のあるフロントエンド（モバイルアプリ、SPA）。REST が向くのはシンプルな CRUD、ファイルアップロード、キャッシュ重視の公開 API です。\n\n両方を併用するハイブリッド構成も一般的です。'),
      ]),
      lesson('schema', 'スキーマ定義', '型、Query、Mutation の SDL を学びます', [
        sec('SDL（Schema Definition Language）', 'GraphQL スキーマは型定義で API の形状を宣言します。スカラー型（String、Int、Boolean）、オブジェクト型、enum、interface があります。', { code: 'type User {\n  id: ID!\n  name: String!\n  email: String!\n  posts: [Post!]!\n}\n\ntype Query {\n  user(id: ID!): User\n  users: [User!]!\n}' }),
        sec('必須と配列', '! は非 null（必須）を意味します。ID! は必須の ID、[Post!]! は Post の非 null 要素の非 null 配列です。null 許容にするかは API 設計の重要な判断です。\n\nリストが空配列と null のどちらを返すかもスキーマで明示します。'),
        sec('Mutation と Subscription', 'Query は読み取り、Mutation は書き込み（作成・更新・削除）、Subscription はリアルタイム更新です。Mutation もフィールド選択でレスポンスを制御できます。', { code: 'type Mutation {\n  createUser(input: CreateUserInput!): User!\n  deleteUser(id: ID!): Boolean!\n}\n\ninput CreateUserInput {\n  name: String!\n  email: String!\n}' }),
      ]),
      lesson('queries', 'クエリの書き方', 'フィールド選択、引数、ネストを学びます', [
        sec('基本クエリ', 'クライアントは必要なフィールドだけを指定します。サーバーは指定されたフィールドだけを解決して返します。', { code: 'query {\n  user(id: "1") {\n    name\n    email\n    posts {\n      title\n      createdAt\n    }\n  }\n}' }),
        sec('引数とエイリアス', 'フィールドに引数を渡せます。エイリアスで同じフィールドを異なる引数で複数回取得できます。', { code: 'query {\n  admin: user(id: "1") { name }\n  guest: user(id: "2") { name }\n  users(limit: 10, offset: 0) { id name }\n}' }),
        sec('フラグメント', '再利用可能なフィールドセットを fragment で定義します。コンポーネントごとに必要なフィールドをフラグメントにまとめるのがベストプラクティスです。', { tip: 'フラグメントはコンポーネントと1対1で定義する Colocation パターンが推奨されます。', code: 'fragment UserInfo on User {\n  id\n  name\n  email\n}\n\nquery {\n  user(id: "1") {\n    ...UserInfo\n    posts { title }\n  }\n}' }),
      ]),
      lesson('resolvers', 'リゾルバ', 'フィールド解決ロジックの実装を学びます', [
        sec('リゾルバの構造', '各フィールドに対応する関数がリゾルバです。(parent, args, context, info) を受け取り、フィールドの値を返します。', { code: 'const resolvers = {\n  Query: {\n    user: (_, { id }, { db }) => db.user.findUnique({ where: { id } }),\n  },\n  User: {\n    posts: (parent, _, { db }) => db.post.findMany({ where: { authorId: parent.id } }),\n  },\n};' }),
        sec('context の活用', 'context はリクエストごとの共有データ（DB 接続、認証ユーザー、DataLoader）を渡します。認証ミドルウェアで context.user をセットするのが定番パターンです。\n\nリゾルバ内で context を参照し、認可チェックや DB アクセスを行います。'),
        sec('N+1 問題と DataLoader', 'User の posts フィールドを各ユーザーごとに個別クエリすると N+1 が発生します。DataLoader でバッチ処理・キャッシュして1クエリにまとめます。\n\nPrisma では findMany + include、または @graphql-tools/batch-execute が使われます。'),
      ]),
      lesson('mutations', 'ミューテーション', 'データの作成・更新・削除を学びます', [
        sec('Mutation の設計', '入力は input 型でまとめ、ペイロードは作成/更新されたオブジェクトを返します。エラーは errors フィールドか GraphQL エラーで表現します。', { code: 'mutation {\n  createPost(input: { title: "Hello", authorId: "1" }) {\n    id\n    title\n    author { name }\n  }\n}' }),
        sec('バリデーション', 'リゾルバ内で入力を検証し、不正な場合は GraphQLError を throw します。Zod や class-validator と組み合わせるのが一般的です。\n\nMutation は副作用があるため、Query より慎重に設計します。冪等性のある更新操作を心がけましょう。'),
        sec('楽観的 UI', 'Apollo Client では update 関数でキャッシュを即座に更新し、サーバーレスポンスで確定します。UX が向上しますが、エラー時のロールバック処理が必要です。', { tip: 'デモで Mutation 実行後にキャッシュがどう更新されるか確認してみてください。' }),
      ]),
      lesson('apollo-server', 'Apollo Server', 'Node.js で GraphQL サーバーを構築します', [
        sec('セットアップ', 'Apollo Server は Node.js 向けの GraphQL サーバーです。スキーマとリゾルバを渡すだけで /graphql エンドポイントが立ち上がります。', { code: 'import { ApolloServer } from "@apollo/server";\nimport { startStandaloneServer } from "@apollo/server/standalone";\n\nconst server = new ApolloServer({ typeDefs, resolvers });\nconst { url } = await startStandaloneServer(server, { listen: { port: 4000 } });' }),
        sec('Express 統合', '既存の Express アプリに Apollo Server をマウントする場合は expressMiddleware を使います。認証ミドルウェアを context に渡すのが一般的です。\n\nCORS 設定やボディサイズ制限も Express 側で管理します。'),
        sec('GraphQL Playground', '開発時は Apollo Sandbox や GraphiQL でクエリを試せます。スキーマのドキュメント自動生成、変数入力、ヘッダー設定が可能です。\n\n本番環境では Playground を無効化するのがセキュリティの基本です。'),
      ]),
      lesson('apollo-client', 'Apollo Client', 'React で GraphQL データを取得します', [
        sec('セットアップ', 'ApolloProvider でアプリをラップし、useQuery / useMutation フックでデータ操作します。キャッシュ管理が自動化されるのが最大の利点です。', { code: 'import { useQuery, gql } from "@apollo/client";\n\nconst GET_USERS = gql`query { users { id name email } }`;\n\nfunction UserList() {\n  const { data, loading, error } = useQuery(GET_USERS);\n  if (loading) return <p>読み込み中...</p>;\n  return <ul>{data.users.map(u => <li key={u.id}>{u.name}</li>)}</ul>;\n}' }),
        sec('キャッシュ戦略', 'Apollo Client は InMemoryCache でレスポンスを正規化保存します。fetchPolicy（cache-first、network-only など）で取得戦略を制御します。\n\ncache-first は初回ネットワーク、以降キャッシュがデフォルト。リアルタイム性が必要なら network-only や polling を使います。'),
        sec('エラーハンドリング', 'error は GraphQL エラーとネットワークエラーを含みます。error.graphQLErrors と error.networkError で分岐処理します。\n\n部分的なデータ（一部フィールドがエラー）も data に含まれるため、null チェックが重要です。'),
      ]),
      lesson('pagination', 'ページネーション', 'Cursor ベースと Offset ベースを学びます', [
        sec('Offset ページネーション', 'limit と offset 引数で実装します。シンプルですが、データ追加・削除時にページずれが起きる問題があります。', { code: 'type Query {\n  users(limit: Int = 10, offset: Int = 0): [User!]!\n}\n\n// リゾルバ\nusers: (_, { limit, offset }, { db }) =>\n  db.user.findMany({ take: limit, skip: offset })' }),
        sec('Cursor ページネーション', 'Relay スタイルの Connection パターンが GraphQL の標準的なページネーションです。pageInfo と edges/nodes で前後ページへのナビゲーションを提供します。\n\nカーソルは通常 ID やエンコードされた位置情報です。無限スクロール UI に最適です。'),
        sec('総件数の取得', 'Offset 方式では count クエリを別途用意します。Cursor 方式では totalCount を Connection に含めるか、別フィールドで提供するかを API 設計で決めます。', { tip: 'デモで Cursor ページネーションの「次のページ」取得を試してみてください。' }),
      ]),
      lesson('best-practices', 'ベストプラクティス', 'スキーマ設計と運用の指針を学びます', [
        sec('スキーマ設計原則', 'フィールド名は camelCase、型名は PascalCase。null 許容は必要な場合のみ。深いネストは3段階程度に抑えます。\n\nバージョニングはフィールド追加（非破壊）を基本とし、削除は @deprecated で段階的に行います。'),
        sec('セキュリティ', 'クエリの深さ制限（max depth）、複雑度制限（complexity analysis）で悪意のある深いクエリを防ぎます。認証・認可はリゾルバレベルで実装します。\n\nIntrospection は本番で無効化するのが一般的です。'),
        sec('パフォーマンス', 'DataLoader で N+1 を解消。Redis キャッシュでホットデータを高速化。Persisted Queries でクエリ文字列の送信を省略します。\n\nフィールドレベルのキャッシュ（@cacheControl）も Apollo Server で利用可能です。'),
      ]),
      lesson('next-steps', '次のステップ', 'GraphQL の学習を続けるための道筋を確認します', [
        sec('次に学ぶこと', '① Prisma 入門 — GraphQL + Prisma の組み合わせ\n② HTTP 入門 — REST との比較を深掘り\n③ Supabase 入門 — GraphQL 対応の BaaS\n④ WebSocket 入門 — Subscription の基盤'),
        sec('学習の道筋', 'GraphQL は「必要なデータだけ取得」という思想がフロントエンド開発と相性抜群です。スキーマ駆動開発、型生成（graphql-codegen）、Apollo のキャッシュ——これらを組み合わせると開発体験が大きく向上します。\n\nおめでとうございます！GraphQL 入門をすべて学びました 🎉', { tip: '小さなブログ API を GraphQL で作り、クエリとミューテーションを Playground で試してみてください。' }),
        sec('実践チェックリスト', '• スキーマで型を明確に定義しているか\n• N+1 を DataLoader で解消しているか\n• クエリの深さ・複雑度制限を設けているか\n• フラグメントでコンポーネントと Colocation しているか\n• 本番で Introspection を無効化しているか'),
      ]),
    ],
  },
  {
    slug: 'storybook',
    componentName: 'StorybookTutorial',
    courseTitle: 'Storybook',
    sidebarTitle: 'Storybook チュートリアル',
    heroAccent: 'Storybook',
    heroSub: 'コンポーネントカタログの作成、Controls、Docs、テスト連携まで。UI 開発を10レッスンで加速します。',
    feature1: 'コンポーネントのバリエーションをインタラクティブに確認できます。',
    icon: '📖',
    logoIcon: '📖',
    section: 'framework',
    subtitle: 'UI コンポーネント開発',
    description: 'Storybook のセットアップ、ストーリー作成、Controls、Docs、Composition、テスト連携まで。コンポーネント駆動開発を10レッスンで学びます。',
    gradient: 'linear-gradient(135deg, #ff4785, #fc521f)',
    lessons: [
      lesson('intro', 'Storybook とは？', 'コンポーネントカタログの概念とメリットを理解します', [
        sec('コンポーネント駆動開発', 'Storybook は UI コンポーネントをアプリ本体から独立して開発・表示・テストする環境です。ボタン1つからページ全体まで、あらゆるコンポーネントの「カタログ」を作れます。\n\nアプリを起動せずにコンポーネントの全バリエーションを確認できるため、開発速度と品質が向上します。'),
        sec('主なメリット', '①デザイナーとエンジニアの共通言語 ②回帰テストの基盤 ③ドキュメントの自動生成 ④エッジケースの視覚的確認。\n\nデザインシステム（Material UI、Chakra UI 等）の開発では Storybook がほぼ必須ツールです。', { tip: 'デモで Button コンポーネントの size・variant バリエーションを切り替えてみてください。' }),
        sec('対応フレームワーク', 'React、Vue、Angular、Svelte、Web Components に対応。Vite や Webpack ベースのビルドも選べます。create-react-app、Next.js、Remix とも統合可能です。'),
      ]),
      lesson('setup', 'セットアップ', 'プロジェクトへの Storybook 導入を学びます', [
        sec('インストール', 'プロジェクトルートで `npx storybook@latest init` を実行すると、フレームワークを自動検出して設定ファイルが生成されます。', { code: 'npx storybook@latest init\n# .storybook/main.ts と preview.ts が生成される\nnpm run storybook  # http://localhost:6006' }),
        sec('設定ファイル', '.storybook/main.ts でストーリーファイルの場所、フレームワーク、addons を設定します。preview.ts でグローバルデコレーター、パラメータ、スタイルを定義します。', { code: '// .storybook/main.ts\nexport default {\n  stories: ["../src/**/*.stories.@(js|ts|tsx)"],\n  addons: ["@storybook/addon-essentials"],\n  framework: "@storybook/react-vite",\n};' }),
        sec('ディレクトリ構成', 'コンポーネントと同じ場所に .stories.tsx を置く Colocation が推奨されます。src/components/Button/Button.tsx と Button.stories.tsx を並べます。\n\n大規模プロジェクトでは stories/ ディレクトリに集約するパターンもあります。'),
      ]),
      lesson('stories', 'ストーリーの書き方', 'CSF（Component Story Format）を学びます', [
        sec('CSF3 の基本', 'Meta オブジェクトでコンポーネントを登録し、named export でストーリー（バリエーション）を定義します。', { code: 'import type { Meta, StoryObj } from "@storybook/react";\nimport { Button } from "./Button";\n\nconst meta: Meta<typeof Button> = {\n  component: Button,\n  title: "Components/Button",\n};\nexport default meta;\n\ntype Story = StoryObj<typeof Button>;\n\nexport const Primary: Story = { args: { variant: "primary", children: "Click me" } };\nexport const Disabled: Story = { args: { ...Primary.args, disabled: true } };' }),
        sec('args による Props 制御', 'args でコンポーネントに渡す props を定義します。ストーリーごとに異なる args を設定し、バリエーションを表現します。\n\nrender 関数でカスタムレンダリングも可能ですが、args ベースが推奨されます。'),
        sec('デコレーター', 'デコレーターはストーリーをラップする関数です。ThemeProvider や Router を提供するのに使います。', { code: 'const meta: Meta = {\n  decorators: [\n    (Story) => (\n      <ThemeProvider theme={lightTheme}>\n        <Story />\n      </ThemeProvider>\n    ),\n  ],\n};' }),
      ]),
      lesson('controls', 'Controls と Actions', 'インタラクティブな Props 操作を学びます', [
        sec('Controls アドオン', 'addon-essentials に含まれる Controls で、ストーリーの args を UI パネルからリアルタイム変更できます。色、テキスト、boolean をインタラクティブに試せます。\n\nargTypes でコントロールの種類（select、color、date）を指定できます。'),
        sec('argTypes の設定', 'props の型に応じたコントロールを自動推論しますが、手動で上書きも可能です。', { code: 'const meta: Meta<typeof Button> = {\n  argTypes: {\n    variant: { control: "select", options: ["primary", "secondary", "danger"] },\n    size: { control: "radio", options: ["sm", "md", "lg"] },\n    onClick: { action: "clicked" },\n  },\n};' }),
        sec('Actions', 'onClick などのイベントハンドラは action: "clicked" で Actions パネルにログ出力されます。ユーザー操作のシミュレーション確認に便利です。', { tip: 'デモで Controls パネルから variant を変更し、コンポーネントの見た目が即座に変わることを確認してみてください。' }),
      ]),
      lesson('docs', '自動ドキュメント', 'Docs アドオンでコンポーネント文書を生成します', [
        sec('Docs ページ', 'autodocs タグを付けると、コンポーネントの Props テーブル、全ストーリー、ソースコードが自動生成されます。', { code: 'const meta: Meta<typeof Button> = {\n  component: Button,\n  tags: ["autodocs"],\n  parameters: {\n    docs: { description: { component: "汎用ボタンコンポーネント" } },\n  },\n};' }),
        sec('MDX ドキュメント', 'Markdown + JSX でリッチなドキュメントページを作成できます。デザインガイドライン、使用例、Do/Don\'t を記述します。\n\nデザインシステムの公式ドキュメントとして MDX ページを充実させるのが一般的です。'),
        sec('Props テーブル', 'TypeScript の型定義から Props テーブルが自動生成されます。JSDoc コメントが description に表示されるため、コンポーネントにコメントを書く習慣が重要です。'),
      ]),
      lesson('composition', 'コンポーネントの合成', '複合コンポーネントのストーリーを学びます', [
        sec('複合ストーリー', 'フォームやカードリストなど、複数コンポーネントを組み合わせたストーリーを render 関数で作成します。', { code: 'export const LoginForm: Story = {\n  render: () => (\n    <form>\n      <Input label="Email" type="email" />\n      <Input label="Password" type="password" />\n      <Button variant="primary">ログイン</Button>\n    </form>\n  ),\n};' }),
        sec('テンプレートストーリー', 'Template ストーリーで共通の render ロジックを定義し、他のストーリーが args だけ変えて再利用するパターンです（CSF2 スタイル）。CSF3 では render + args の組み合わせが主流です。'),
        sec('モックデータ', 'MSW（Mock Service Worker）やデコレーターで API レスポンスをモックし、データ依存コンポーネントを独立して表示できます。\n\n実際の API に依存しないため、開発とテストが安定します。'),
      ]),
      lesson('testing', 'テスト連携', 'Storybook とテストツールの統合を学びます', [
        sec('インタラクションテスト', '@storybook/addon-interactions と @storybook/test で、ストーリー内のユーザー操作をテストします。play 関数でクリック・入力を自動化します。', { code: 'export const SubmitForm: Story = {\n  play: async ({ canvas, userEvent }) => {\n    await userEvent.type(canvas.getByLabelText("Email"), "test@example.com");\n    await userEvent.click(canvas.getByRole("button", { name: "送信" }));\n    await expect(canvas.getByText("送信完了")).toBeInTheDocument();\n  },\n};' }),
        sec('Visual Regression Testing', 'Chromatic（Storybook 公式）や Playwright でスクリーンショット比較テストを実行します。意図しない UI 変更を自動検出できます。\n\nCI でストーリーのスクリーンショットを撮影し、前回との差分をレビューするフローが一般的です。'),
        sec('テストランナー', 'storybook test-runner で全ストーリーの smoke テストを実行します。各ストーリーがエラーなくレンダリングされることを確認する回帰テストの基盤になります。'),
      ]),
      lesson('addons', 'アドオン', '便利なアドオンの活用を学びます', [
        sec('Essential アドオン', 'addon-essentials には Controls、Actions、Docs、Viewport、Backgrounds、Measure、Outline が含まれます。ほとんどのプロジェクトでこれだけで十分です。\n\nViewport でモバイル・タブレット表示を確認、Backgrounds でダークモード背景をテストできます。'),
        sec('a11y アドオン', '@storybook/addon-a11y でアクセシビリティチェックを実行します。ARIA 属性の不足、コントラスト比の問題を自動検出します。\n\nコンポーネント開発の早い段階で a11y 問題を発見できるのが大きなメリットです。'),
        sec('カスタムアドオン', '独自のアドオンを作成して、デザイントークンの切り替えや API モック状態の管理など、プロジェクト固有の機能を追加できます。', { tip: 'デモで Viewport を切り替え、レスポンシブデザインを確認してみてください。' }),
      ]),
      lesson('workflow', '開発ワークフロー', 'チームでの Storybook 活用を学びます', [
        sec('デザインレビュー', 'Storybook を Vercel や Chromatic にデプロイし、PR ごとにプレビュー URL を共有します。デザイナーがブラウザでレビューし、コメントを残せます。\n\nFigma との連携（Design Addon）でデザインと実装の比較も可能です。'),
        sec('CI/CD 統合', 'GitHub Actions で Storybook をビルド・デプロイし、Visual Regression テストを実行します。UI 変更はスクリーンショット差分でレビューされます。', { code: '# .github/workflows/storybook.yml\n- run: npm run build-storybook\n- uses: chromaui/action@v1\n  with: projectToken: ${{ secrets.CHROMATIC_TOKEN }}' }),
        sec('デザインシステム', 'Storybook はデザインシステムの「生きたドキュメント」です。コンポーネント、トークン、ガイドラインを一元管理し、チーム全体で共有します。\n\nStorybook 8 ではテーマ切り替えやレスポンシブプレビューがさらに強化されています。'),
      ]),
      lesson('next-steps', '次のステップ', 'Storybook の学習を続けるための道筋を確認します', [
        sec('次に学ぶこと', '① Playwright 入門 — E2E テストとの連携\n② CSSアニメーション — コンポーネントのモーション\n③ i18n 入門 — 多言語ストーリーの作成\n④ アクセシビリティ — a11y テストの深化'),
        sec('学習の道筋', 'Storybook は「コンポーネントを独立して磨く」文化を育てます。1コンポーネント1ストーリーから始め、Controls・Docs・テストを段階的に追加しましょう。\n\nおめでとうございます！Storybook 入門をすべて学びました 🎉', { tip: '既存プロジェクトの主要コンポーネントにストーリーを追加してみてください。' }),
        sec('実践チェックリスト', '• 全コンポーネントにストーリーがあるか\n• Controls で主要 props を操作できるか\n• autodocs で Props テーブルが生成されているか\n• a11y チェックを CI に組み込んでいるか\n• Chromatic 等でビジュアルテストしているか'),
      ]),
    ],
  },
  {
    slug: 'supabase',
    componentName: 'SupabaseTutorial',
    courseTitle: 'Supabase',
    sidebarTitle: 'Supabase チュートリアル',
    heroAccent: 'Supabase BaaS',
    heroSub: 'PostgreSQL、認証、ストレージ、Realtime を統合した BaaS。バックエンドを10レッスンで素早く構築します。',
    feature1: '認証フローとリアルタイムデータ同期をデモで体験できます。',
    icon: '⚡',
    logoIcon: '⚡',
    section: 'framework',
    subtitle: 'オープンソース BaaS',
    description: 'Supabase のセットアップ、認証、データベース、ストレージ、Realtime、RLS、Edge Functions まで。Firebase の代替を10レッスンで学びます。',
    gradient: 'linear-gradient(135deg, #3ecf8e, #1a7f5a)',
    lessons: [
      lesson('intro', 'Supabase とは？', 'BaaS の概念と Supabase の機能を理解します', [
        sec('BaaS とは', 'BaaS（Backend as a Service）は、バックエンドのインフラ（DB、認証、ストレージ）をクラウドで提供するサービスです。サーバー構築・運用を省略し、アプリ開発に集中できます。\n\nSupabase は「オープンソースの Firebase 代替」として2020年に登場。PostgreSQL ベースで、SQL の力を活かせます。'),
        sec('主要機能', '• Database — PostgreSQL（SQL、リレーション、全文検索）\n• Auth — メール、OAuth、Magic Link\n• Storage — ファイルアップロード（S3 互換）\n• Realtime — DB 変更の WebSocket 配信\n• Edge Functions — Deno ベースのサーバーレス関数', { tip: 'デモで Supabase Dashboard の Table Editor を開き、データを直接操作してみてください。' }),
        sec('Firebase との比較', 'Firebase は NoSQL（Firestore）、Supabase は SQL（PostgreSQL）。リレーショナルデータや複雑なクエリが必要なら Supabase が有利です。リアルタイム、認証、ホスティングは両者とも提供します。'),
      ]),
      lesson('setup', 'セットアップ', 'プロジェクト作成とクライアント初期化を学びます', [
        sec('プロジェクト作成', 'supabase.com でアカウント作成し、New Project からプロジェクトを作成します。リージョン（東京: ap-northeast-1）と DB パスワードを設定します。', { code: 'npm install @supabase/supabase-js\n\nimport { createClient } from "@supabase/supabase-js";\nconst supabase = createClient(\n  process.env.SUPABASE_URL,\n  process.env.SUPABASE_ANON_KEY\n);' }),
        sec('環境変数', 'SUPABASE_URL と SUPABASE_ANON_KEY を .env に設定します。anon key はフロントエンドに公開可能ですが、RLS でデータアクセスを制御します。\n\nservice_role key はサーバー側のみで使用し、RLS をバイパスします。絶対にフロントに露出しないでください。'),
        sec('ローカル開発', 'Supabase CLI でローカルに Supabase スタックを起動できます。`npx supabase init` → `npx supabase start` で Docker 上に PostgreSQL + Auth + Storage が立ち上がります。'),
      ]),
      lesson('database', 'データベース操作', 'テーブル作成と CRUD クエリを学びます', [
        sec('テーブル作成', 'Dashboard の Table Editor または SQL Editor でテーブルを作成します。RLS を有効にするのが Supabase の鉄則です。', { code: 'create table posts (\n  id uuid default gen_random_uuid() primary key,\n  title text not null,\n  content text,\n  user_id uuid references auth.users(id),\n  created_at timestamptz default now()\n);\nalter table posts enable row level security;' }),
        sec('CRUD 操作', 'supabase-js のチェーン API でデータ操作します。型安全にするには supabase gen types で TypeScript 型を生成します。', { code: 'const { data, error } = await supabase\n  .from("posts")\n  .select("*, profiles(name, avatar_url)")\n  .eq("user_id", userId)\n  .order("created_at", { ascending: false });' }),
        sec('フィルタとページネーション', '.eq()、.gt()、.like()、.in() でフィルタ。.range(from, to) でページネーション。.single() で1件取得します。\n\n複雑なクエリは RPC（ストアドプロシージャ）を Database Functions として定義し、.rpc() で呼び出します。'),
      ]),
      lesson('auth', '認証', 'メール認証と OAuth ログインを学びます', [
        sec('メール認証', 'signUp でユーザー登録、signInWithPassword でログインします。確認メールは Dashboard の Auth 設定でカスタマイズできます。', { code: 'const { data, error } = await supabase.auth.signUp({\n  email: "user@example.com",\n  password: "secure-password",\n});\n\nconst { data: session } = await supabase.auth.signInWithPassword({\n  email, password,\n});' }),
        sec('OAuth プロバイダー', 'Dashboard → Authentication → Providers で Google、GitHub 等を有効化します。signInWithOAuth でリダイレクトベースのログインを実装します。', { code: 'await supabase.auth.signInWithOAuth({\n  provider: "github",\n  options: { redirectTo: "https://myapp.com/auth/callback" },\n});' }),
        sec('セッション管理', 'onAuthStateChange でログイン/ログアウトを監視します。getSession() で現在のセッションを取得。signOut() でログアウトします。\n\nReact では Supabase Auth Helpers（@supabase/auth-helpers-nextjs）で SSR 対応のセッション管理が簡単です。', { tip: 'デモでログイン → セッション取得 → ログアウトのフローを追跡してみてください。' }),
      ]),
      lesson('rls', 'Row Level Security', 'データベースレベルのアクセス制御を学びます', [
        sec('RLS の概念', 'Row Level Security は PostgreSQL の機能で、行単位のアクセス制御を SQL ポリシーで定義します。Supabase では anon key でも RLS により安全にデータアクセスできます。\n\nRLS を有効にしないテーブルは anon key で全データが読み書き可能——必ず有効化してください。'),
        sec('ポリシーの作成', 'auth.uid() で現在のユーザー ID を参照し、自分のデータだけアクセス可能にします。', { code: 'create policy "Users can read own posts"\n  on posts for select\n  using (auth.uid() = user_id);\n\ncreate policy "Users can insert own posts"\n  on posts for insert\n  with check (auth.uid() = user_id);' }),
        sec('ポリシーのテスト', 'Dashboard の RLS ポリシーエディタでテストできます。異なるロール（anon、authenticated、service_role）でクエリを試し、期待通りのアクセス制御を確認します。\n\n複雑なポリシーは段階的に追加し、各ポリシーを個別にテストしましょう。'),
      ]),
      lesson('storage', 'ファイルストレージ', '画像・ファイルのアップロードを学びます', [
        sec('バケット作成', 'Dashboard → Storage でバケットを作成します。public バケットは URL で直接アクセス可能、private は認証が必要です。', { code: 'const { data, error } = await supabase.storage\n  .from("avatars")\n  .upload(`${userId}/avatar.png`, file, {\n    cacheControl: "3600",\n    upsert: true,\n  });' }),
        sec('ファイル取得', 'getPublicUrl() で公開 URL を取得。private バケットは createSignedUrl() で期限付き URL を生成します。\n\n画像リサイズは Supabase Storage の transform 機能（Pro プラン）や、Edge Function で処理するパターンがあります。'),
        sec('Storage ポリシー', 'RLS と同様、Storage にもポリシーを設定します。ユーザーは自分のフォルダにのみアップロード可能にするのが一般的です。', { code: 'create policy "Users can upload own avatar"\n  on storage.objects for insert\n  with check (bucket_id = \'avatars\' and auth.uid()::text = (storage.foldername(name))[1]);' }),
      ]),
      lesson('realtime', 'リアルタイム機能', 'DB 変更の WebSocket 配信を学びます', [
        sec('Realtime の仕組み', 'Supabase Realtime は PostgreSQL の変更（INSERT、UPDATE、DELETE）を WebSocket でクライアントに配信します。チャット、通知、共同編集に最適です。', { code: 'const channel = supabase\n  .channel("posts-changes")\n  .on("postgres_changes", {\n    event: "INSERT",\n    schema: "public",\n    table: "posts",\n  }, (payload) => {\n    console.log("新しい投稿:", payload.new);\n    setPosts(prev => [payload.new, ...prev]);\n  })\n  .subscribe();' }),
        sec('Presence', 'チャンネルに参加しているユーザーのオンライン状態を共有できます。共同編集アプリで「誰が今見ているか」を表示するのに使います。'),
        sec('Broadcast', 'クライアント間でカスタムメッセージを送受信します。カーソル位置の共有やタイピングインジケーターなど、DB を介さないリアルタイム通信に使います。', { tip: 'デモで2つのブラウザタブを開き、一方でデータを追加して他方にリアルタイム反映されることを確認してみてください。' }),
      ]),
      lesson('edge-functions', 'Edge Functions', 'サーバーレス関数の作成を学びます', [
        sec('Edge Functions とは', 'Deno ベースのサーバーレス関数で、グローバルに低レイテンシで実行されます。Webhook 処理、サードパーティ API 連携、複雑なビジネスロジックに使います。', { code: 'npx supabase functions new hello-world\nnpx supabase functions serve hello-world\nnpx supabase functions deploy hello-world' }),
        sec('関数の実装', 'Deno.serve で HTTP ハンドラを定義します。CORS ヘッダーの設定、Supabase クライアントの初期化が基本パターンです。', { code: 'import { createClient } from "https://esm.sh/@supabase/supabase-js@2";\n\nDeno.serve(async (req) => {\n  const supabase = createClient(url, serviceKey);\n  const { data } = await supabase.from("posts").select("*");\n  return new Response(JSON.stringify(data), {\n    headers: { "Content-Type": "application/json" },\n  });\n});' }),
        sec('Webhook と Cron', 'Stripe の Webhook を Edge Function で受信し、DB を更新するパターンが一般的です。pg_cron と組み合わせて定期処理も可能です。'),
      ]),
      lesson('deployment', 'デプロイと運用', '本番環境のセットアップを学びます', [
        sec('マイグレーション', 'Supabase CLI でスキーマ変更をマイグレーションファイルとして管理します。`npx supabase db diff` で差分を生成し、Git で管理します。', { code: 'npx supabase migration new add_comments_table\nnpx supabase db push  # ローカル → リモート\nnpx supabase db pull  # リモート → ローカル' }),
        sec('環境の分離', '開発・ステージング・本番で別プロジェクトを作成するのがベストプラクティスです。マイグレーションをステージングで検証してから本番に適用します。\n\nSupabase Branching（Pro プラン）で Git ブランチと DB ブランチを連携できます。'),
        sec('モニタリング', 'Dashboard の Logs で API、Auth、Storage、Realtime のログを確認します。Database の Query Performance で遅いクエリを特定し、インデックスを追加します。'),
      ]),
      lesson('next-steps', '次のステップ', 'Supabase の学習を続けるための道筋を確認します', [
        sec('次に学ぶこと', '① 認証入門 — 認証の理論を深掘り\n② Prisma 入門 — Supabase PostgreSQL + Prisma\n③ WebSocket 入門 — Realtime の仕組み\n④ Docker 入門 — ローカル Supabase スタック'),
        sec('学習の道筋', 'Supabase は「バックエンドを素早く立ち上げる」最強のツールです。RLS でセキュリティを担保し、Realtime でリアルタイム機能を追加——フルスタック開発の速度が劇的に上がります。\n\nおめでとうございます！Supabase 入門をすべて学びました 🎉', { tip: '小さな TODO アプリを Supabase で作り、認証 + CRUD + Realtime を一通り実装してみてください。' }),
        sec('実践チェックリスト', '• 全テーブルで RLS を有効化しているか\n• service_role key をフロントに露出していないか\n• マイグレーションを Git 管理しているか\n• Storage ポリシーを設定しているか\n• 本番と開発でプロジェクトを分離しているか'),
      ]),
    ],
  },
  {
    slug: 'playwright',
    componentName: 'PlaywrightTutorial',
    courseTitle: 'Playwright',
    sidebarTitle: 'Playwright チュートリアル',
    heroAccent: 'Playwright E2E',
    heroSub: 'ブラウザ自動化、セレクタ、テスト設計、CI 連携まで。信頼性の高い E2E テストを10レッスンで学びます。',
    feature1: 'テスト実行の録画とトレースビューアでデバッグできます。',
    icon: '🎭',
    logoIcon: '🎭',
    section: 'framework',
    subtitle: 'E2E テスト自動化',
    description: 'Playwright のセットアップ、セレクタ、アサーション、Page Object Model、API テスト、CI 連携まで。E2E テストを10レッスンで学びます。',
    gradient: 'linear-gradient(135deg, #2ead33, #1a7a1e)',
    lessons: [
      lesson('intro', 'Playwright とは？', 'E2E テストの概念と Playwright の特徴を理解します', [
        sec('E2E テストとは', 'End-to-End テストは、ユーザーの操作（クリック、入力、ナビゲーション）を自動化し、アプリ全体が正しく動作するか検証します。ユニットテストが部品を、E2E テストが製品全体をテストします。\n\n「ログインして商品をカートに入れて決済する」といったユーザーシナリオを自動化できます。'),
        sec('Playwright の特徴', 'Microsoft が開発。Chromium、Firefox、WebKit の3ブラウザをサポート。自動待機（auto-wait）で flaky テストを削減。Codegen で操作を録画してテスト生成。\n\nSelenium や Cypress と比べ、マルチブラウザ対応、並列実行、トレースビューアが強みです。', { tip: 'デモで Codegen を起動し、ブラウザ操作を録画してテストコードを生成してみてください。' }),
        sec('テストピラミッド', 'ユニットテスト（多・速・安）→ 統合テスト → E2E テスト（少・遅・高コスト）のピラミッド構造が推奨されます。E2E はクリティカルパスに集中し、細かいロジックはユニットテストに任せます。'),
      ]),
      lesson('setup', 'セットアップ', 'Playwright のインストールと初期設定を学びます', [
        sec('インストール', '`npm init playwright@latest` でプロジェクトに Playwright を追加します。テストディレクトリ、設定ファイル、サンプルテストが生成されます。', { code: 'npm init playwright@latest\n# tests/ ディレクトリと playwright.config.ts が生成\nnpx playwright test          # テスト実行\nnpx playwright test --ui     # UI モード' }),
        sec('設定ファイル', 'playwright.config.ts でブラウザ、ベース URL、タイムアウト、リトライ、スクリーンショット設定を行います。', { code: 'export default defineConfig({\n  testDir: "./tests",\n  fullyParallel: true,\n  retries: process.env.CI ? 2 : 0,\n  use: {\n    baseURL: "http://localhost:3000",\n    trace: "on-first-retry",\n    screenshot: "only-on-failure",\n  },\n});' }),
        sec('プロジェクト構成', 'tests/ にテストファイル（*.spec.ts）、tests/fixtures/ にテストデータ、tests/pages/ に Page Object を配置するのが一般的です。\n\nwebServer オプションでテスト実行前に開発サーバーを自動起動できます。'),
      ]),
      lesson('selectors', 'セレクタ', '要素の特定方法とベストプラクティスを学びます', [
        sec('推奨セレクタ', 'Playwright はユーザーが操作する方法で要素を特定することを推奨します。優先順位: getByRole > getByLabel > getByText > getByTestId > CSS/XPath。', { code: 'await page.getByRole("button", { name: "ログイン" }).click();\nawait page.getByLabel("メールアドレス").fill("user@example.com");\nawait page.getByText("送信完了").isVisible();\nawait page.getByTestId("submit-button").click();' }),
        sec('ロケーターのチェーン', 'locator() でスコープを絞り、filter() で条件を追加できます。親要素の中の特定の子要素を取得するのに便利です。', { code: 'const row = page.getByRole("row").filter({ hasText: "太郎" });\nawait row.getByRole("button", { name: "編集" }).click();' }),
        sec('セレクタのベストプラクティス', 'data-testid 属性をコンポーネントに付与し、getByTestId で参照するのが安定したテストの基本です。CSS クラス名や DOM 構造に依存するセレクタはリファクタリングで壊れやすいです。\n\nアクセシビリティ属性（role、label）を正しく設定すれば、getByRole/getByLabel が自然に使えます。'),
      ]),
      lesson('actions', '操作', 'クリック、入力、ナビゲーションを学びます', [
        sec('基本操作', 'click()、fill()、press()、check()、selectOption() でユーザー操作を再現します。Playwright は要素が操作可能になるまで自動待機します。', { code: 'await page.goto("/login");\nawait page.getByLabel("Email").fill("test@example.com");\nawait page.getByLabel("Password").fill("password123");\nawait page.getByRole("button", { name: "ログイン" }).click();\nawait page.waitForURL("/dashboard");' }),
        sec('ファイル操作', 'setInputFiles() でファイルアップロードをテスト。download イベントでダウンロードを検証します。', { code: 'await page.getByLabel("ファイル選択").setInputFiles("test-image.png");\nconst download = await page.waitForEvent("download");\nexpect(download.suggestedFilename()).toBe("report.pdf");' }),
        sec('マルチタブ・フレーム', 'context.newPage() で新タブを開く。frameLocator() で iframe 内の要素を操作します。\n\nポップアップウィンドウは page.waitForEvent("popup") で取得し、同様に操作できます。'),
      ]),
      lesson('assertions', 'アサーション', '状態の検証方法を学びます', [
        sec('expect の基本', '@playwright/test の expect は自動リトライ付きアサーションです。要素の表示、テキスト、属性、URL を検証します。', { code: 'await expect(page.getByRole("heading")).toHaveText("ダッシュボード");\nawait expect(page.getByTestId("user-count")).toHaveText("42");\nawait expect(page).toHaveURL(/dashboard/);\nawait expect(page.getByRole("button")).toBeEnabled();' }),
        sec('ソフトアサーション', 'expect.soft() は失敗してもテストを続行し、最後にまとめて報告します。複数の UI 要素を一度にチェックするのに便利です。', { code: 'await expect.soft(page.getByText("タイトル")).toBeVisible();\nawait expect.soft(page.getByText("説明")).toBeVisible();\nawait expect.soft(page.getByRole("img")).toHaveAttribute("alt", "商品画像");' }),
        sec('スクリーンショット比較', 'toHaveScreenshot() でビジュアルリグレッションテストを実行します。初回実行でベースライン画像を生成し、以降は差分を検出します。', { tip: 'デモで --ui モードでテストを実行し、各ステップのスクリーンショットを確認してみてください。' }),
      ]),
      lesson('page-object', 'Page Object Model', 'テストコードの構造化パターンを学びます', [
        sec('POM の概念', 'Page Object Model はページごとにクラスを作り、セレクタと操作をカプセル化するパターンです。テストコードが読みやすく、UI 変更時の修正箇所が局所化されます。', { code: 'class LoginPage {\n  constructor(private page: Page) {}\n  async goto() { await this.page.goto("/login"); }\n  async login(email: string, password: string) {\n    await this.page.getByLabel("Email").fill(email);\n    await this.page.getByLabel("Password").fill(password);\n    await this.page.getByRole("button", { name: "ログイン" }).click();\n  }\n}' }),
        sec('テストでの利用', 'Page Object を使うテストはシナリオが明確に読めます。', { code: 'test("ログインしてダッシュボードを表示", async ({ page }) => {\n  const loginPage = new LoginPage(page);\n  await loginPage.goto();\n  await loginPage.login("test@example.com", "password");\n  await expect(page.getByRole("heading")).toHaveText("ダッシュボード");\n});' }),
        sec('Fixture との組み合わせ', 'Playwright の fixture で Page Object を自動注入できます。テストごとにインスタンスを生成する手間が省けます。'),
      ]),
      lesson('api-testing', 'API テスト', 'Playwright で API を直接テストします', [
        sec('request コンテキスト', 'Playwright はブラウザ操作に加え、API リクエストも送信できます。E2E テストの前処理（データセットアップ）や API 単体テストに使います。', { code: 'test("API でユーザー作成", async ({ request }) => {\n  const response = await request.post("/api/users", {\n    data: { name: "テスト太郎", email: "test@example.com" },\n  });\n  expect(response.ok()).toBeTruthy();\n  const user = await response.json();\n  expect(user.name).toBe("テスト太郎");\n});' }),
        sec('認証付き API テスト', 'storageState や request の headers で認証トークンを付与します。ログイン API でトークンを取得し、以降の API テストで再利用するパターンです。'),
        sec('E2E + API の組み合わせ', 'API でテストデータをセットアップ → ブラウザで UI をテスト → API でクリーンアップ。高速で安定した E2E テストが書けます。'),
      ]),
      lesson('ci', 'CI 連携', 'GitHub Actions でテストを自動実行します', [
        sec('GitHub Actions 設定', 'PR や push 時に Playwright テストを自動実行します。失敗時のスクリーンショットとトレースを Artifact として保存します。', { code: '# .github/workflows/playwright.yml\n- uses: actions/checkout@v4\n- uses: actions/setup-node@v4\n- run: npm ci\n- run: npx playwright install --with-deps\n- run: npx playwright test\n- uses: actions/upload-artifact@v4\n  if: failure()\n  with: { name: playwright-report, path: playwright-report/ }' }),
        sec('並列実行', 'Playwright は fullyParallel: true でテストファイルを並列実行します。sharding で CI ジョブを分割し、実行時間を短縮できます。', { code: 'npx playwright test --shard=1/3  # 3分割の1番目\nnpx playwright test --shard=2/3\nnpx playwright test --shard=3/3' }),
        sec('Docker での実行', 'mcr.microsoft.com/playwright の公式 Docker イメージを使い、CI 環境でブラウザ依存を確実に解決します。'),
      ]),
      lesson('debugging', 'デバッグ', 'テスト失敗の原因を特定する方法を学びます', [
        sec('UI モード', '`npx playwright test --ui` でインタラクティブなテストランナーを起動。ステップごとに実行、DOM スナップショット、ネットワークログを確認できます。'),
        sec('トレースビューア', 'trace: "on-first-retry" 設定で、失敗時のトレースファイルを生成。`npx playwright show-trace trace.zip` でタイムライン形式のデバッグ UI が開きます。\n\n各アクションの前後のスクリーンショット、ネットワークリクエスト、コンソールログが時系列で表示されます。'),
        sec('デバッグの Tips', 'page.pause() でテストを一時停止し、Playwright Inspector で操作を試せます。--debug フラグでステップ実行モードになります。\n\nflaky テストは retries を増やすのではなく、根本原因（タイミング依存、不安定なセレクタ）を修正しましょう。', { tip: 'デモで意図的に失敗するテストを書き、トレースビューアで原因を特定してみてください。' }),
      ]),
      lesson('next-steps', '次のステップ', 'Playwright の学習を続けるための道筋を確認します', [
        sec('次に学ぶこと', '① Storybook 入門 — コンポーネントテストとの連携\n② Webセキュリティ — テストでのセキュリティ検証\n③ Docker 入門 — CI 用のテスト環境\n④ 認証入門 — 認証フローの E2E テスト'),
        sec('学習の道筋', 'Playwright は「ユーザーが実際に使う方法でテストする」思想が核心です。getByRole を優先し、Page Object で構造化し、CI で自動化——この3つで信頼性の高いテストスイートが作れます。\n\nおめでとうございます！Playwright 入門をすべて学びました 🎉', { tip: '自分のアプリのログインフローを Playwright で自動テストしてみてください。' }),
        sec('実践チェックリスト', '• getByRole/getByLabel を優先しているか\n• data-testid を主要コンポーネントに付与しているか\n• Page Object でテストを構造化しているか\n• CI でテストを自動実行しているか\n• 失敗時のトレースを保存しているか'),
      ]),
    ],
  },
  {
    slug: 'security',
    componentName: 'SecurityTutorial',
    courseTitle: 'Webセキュリティ',
    sidebarTitle: 'Webセキュリティ チュートリアル',
    heroAccent: 'Web Security',
    heroSub: 'XSS、CSRF、SQL インジェクション、HTTPS、セキュリティヘッダーまで。攻撃手法と防御策を10レッスンで学びます。',
    feature1: '脆弱性のあるコードと安全なコードの比較をデモで確認できます。',
    icon: '🛡️',
    logoIcon: '🛡️',
    section: 'practice',
    subtitle: '攻撃と防御の基礎',
    description: 'OWASP Top 10、XSS、CSRF、SQL インジェクション、HTTPS、セキュリティヘッダー、認証の脆弱性まで。Web アプリのセキュリティを10レッスンで学びます。',
    gradient: 'linear-gradient(135deg, #dc2626, #991b1b)',
    lessons: [
      lesson('intro', 'Webセキュリティとは？', 'セキュリティの重要性と OWASP Top 10 を理解します', [
        sec('なぜセキュリティが重要か', 'Web アプリの脆弱性は、個人情報漏洩、不正アクセス、サービス停止など深刻な被害を引き起こします。Equifax や Heartland など、大企業のデータ漏洩事件も脆弱な Web アプリが原因でした。\n\n開発者は「機能を作る」だけでなく「安全に作る」責任があります。セキュリティは後付けではなく、設計段階から組み込むべきです。'),
        sec('OWASP Top 10', 'OWASP（Open Web Application Security Project）が定期的に更新する、Web アプリの最重要リスク10選です。2021年版の上位は: ①アクセス制御の不備 ②暗号化の失敗 ③インジェクション ④安全でない設計 ⑤セキュリティの設定ミス。\n\nこのチュートリアルでは、特にインジェクション（XSS、SQLi）、認証の不備、設定ミスを重点的に学びます。', { tip: 'デモで OWASP Top 10 の各項目と具体例の対応を確認してみてください。' }),
        sec('セキュリティの基本原則', '最小権限の原則——必要最小限の権限だけを付与。防御の深層化——複数の防御層を重ねる。安全なデフォルト——設定なしで安全な状態。入力は信用しない——すべてのユーザー入力を検証・サニタイズします。'),
      ]),
      lesson('xss', 'XSS（クロスサイトスクリプティング）', '反射型・格納型 XSS の攻撃と防御を学びます', [
        sec('XSS の仕組み', 'XSS は攻撃者が悪意のある JavaScript をページに注入し、他のユーザーのブラウザで実行させる攻撃です。Cookie の窃取、キーロガー、ページ改ざんが可能になります。\n\n反射型（URL パラメータに含まれる）、格納型（DB に保存され他ユーザーに表示）、DOM 型（クライアント側 JS の脆弱性）の3種類があります。'),
        sec('防御策', '①出力時のエスケープ——HTML エンティティに変換。②Content-Security-Policy（CSP）ヘッダー——許可されたソースのスクリプトのみ実行。③HttpOnly Cookie——JS から Cookie にアクセス不可。', { code: '// 危険: innerHTML にユーザー入力を直接挿入\nelement.innerHTML = userInput;\n\n// 安全: textContent を使うか、DOMPurify でサニタイズ\nelement.textContent = userInput;\n// または\nelement.innerHTML = DOMPurify.sanitize(userInput);' }),
        sec('React での XSS', 'React はデフォルトで JSX 内の値をエスケープしますが、dangerouslySetInnerHTML は危険です。ユーザー入力を HTML として表示する必要がある場合のみ DOMPurify を使います。\n\nCSP ヘッダーを設定し、インラインスクリプトを禁止するのが最強の防御です。', { code: 'Content-Security-Policy: default-src \'self\'; script-src \'self\'; style-src \'self\' \'unsafe-inline\'' }),
      ]),
      lesson('csrf', 'CSRF（クロスサイトリクエストフォージェリ）', 'CSRF 攻撃の仕組みと防御を学びます', [
        sec('CSRF の仕組み', 'CSRF は、ログイン済みユーザーのブラウザを利用して、意図しないリクエストを送信させる攻撃です。悪意のあるサイトに <img src="https://bank.com/transfer?to=attacker&amount=10000"> を埋め込む例が古典的です。\n\nCookie ベースのセッション認証では、ブラウザが自動的に Cookie を送信するため CSRF の影響を受けます。'),
        sec('防御策', '①CSRF トークン——フォームにランダムトークンを埋め込み、サーバーが照合。②SameSite Cookie——クロスサイトリクエストでの Cookie 送信を制限。③重要操作に再認証を要求。', { code: '// サーバー側: トークン生成\nconst csrfToken = crypto.randomBytes(32).toString("hex");\nreq.session.csrfToken = csrfToken;\n\n// フォーム: hidden フィールド\n<input type="hidden" name="_csrf" value={csrfToken} />\n\n// 検証\nif (req.body._csrf !== req.session.csrfToken) {\n  return res.status(403).json({ error: "CSRF token invalid" });\n}' }),
        sec('SameSite の設定', 'SameSite=Strict は最も厳格（クロスサイトからの Cookie 送信を完全禁止）。Lax は GET ナビゲーションのみ許可（デフォルト推奨）。None は Secure 必須で、クロスサイトでも送信（サードパーティ Cookie 用）。\n\n新規プロジェクトでは SameSite=Lax が安全なデフォルトです。'),
      ]),
      lesson('sql-injection', 'SQL インジェクション', 'SQL インジェクションの攻撃と防御を学びます', [
        sec('SQL インジェクションの仕組み', 'ユーザー入力を SQL クエリに直接埋め込むと、攻撃者が SQL コマンドを注入できます。`\' OR 1=1 --` で全ユーザーのデータを取得、`; DROP TABLE users;` でテーブル削除も可能です。\n\nOWASP Top 10 で長年上位にランクされる、最も有名な Web 脆弱性です。'),
        sec('防御策: パラメータ化クエリ', 'プレースホルダー（? や $1）を使い、ユーザー入力を SQL 構造の一部として扱わせません。ORM（Prisma 等）を使うと自動的にパラメータ化されます。', { code: '// 危険: 文字列連結\nconst query = `SELECT * FROM users WHERE email = \'${email}\'`;\n\n// 安全: パラメータ化クエリ\nconst query = "SELECT * FROM users WHERE email = $1";\nconst result = await db.query(query, [email]);' }),
        sec('ORM とバリデーション', 'Prisma や Drizzle などの ORM はパラメータ化クエリを自動使用します。生 SQL を使う場合は必ず $queryRaw のプレースホルダーを使います。\n\n入力バリデーション（Zod 等）も重要ですが、SQL インジェクションの主防御はパラメータ化クエリです。バリデーションだけでは不十分な場合があります。', { tip: 'デモで SQL インジェクションのペイロードを入力し、パラメータ化クエリが防御することを確認してみてください。' }),
      ]),
      lesson('https', 'HTTPS と暗号化', 'TLS/SSL と通信の暗号化を学びます', [
        sec('HTTPS の重要性', 'HTTPS（HTTP over TLS）は通信を暗号化し、盗聴・改ざん・なりすましを防ぎます。パスワード、Cookie、個人情報は平文 HTTP では丸見えです。\n\n2018年以降、主要ブラウザは HTTP サイトに「安全ではありません」警告を表示します。HTTPS は必須です。'),
        sec('TLS ハンドシェイク', '①クライアントが ClientHello（対応する暗号スイート）を送信 → ②サーバーが証明書と ServerHello を返す → ③クライアントが証明書を検証 → ④共通の暗号鍵を交換 → ⑤暗号化通信開始。\n\nLet\'s Encrypt で無料の SSL 証明書を取得でき、Vercel/Netlify 等のホスティングは自動で HTTPS を提供します。'),
        sec('HSTS', 'HTTP Strict Transport Security ヘッダーで、ブラウザに「このサイトは常に HTTPS でアクセスすること」を指示します。中間者攻撃（SSL stripping）を防ぎます。', { code: 'Strict-Transport-Security: max-age=31536000; includeSubDomains; preload' }),
      ]),
      lesson('headers', 'セキュリティヘッダー', '重要な HTTP セキュリティヘッダーを学びます', [
        sec('主要ヘッダー一覧', '• Content-Security-Policy — XSS 防御\n• X-Content-Type-Options: nosniff — MIME スニッフィング防止\n• X-Frame-Options: DENY — クリックジャッキング防止\n• Referrer-Policy — リファラー情報の制御\n• Permissions-Policy — ブラウザ機能の制限'),
        sec('CSP の設定', 'Content-Security-Policy は許可するリソースのソースを制限します。最も効果的な XSS 防御策の1つです。', { code: 'Content-Security-Policy:\n  default-src \'self\';\n  script-src \'self\' https://cdn.example.com;\n  style-src \'self\' \'unsafe-inline\';\n  img-src \'self\' data: https:;\n  frame-ancestors \'none\';' }),
        sec('helmet.js', 'Express では helmet ミドルウェアでセキュリティヘッダーを一括設定できます。Next.js では next.config.js の headers 設定で追加します。\n\nsecurityheaders.com でヘッダーの設定を評価できます。A+ 評価を目指しましょう。', { code: 'import helmet from "helmet";\napp.use(helmet());\n// または個別設定\napp.use(helmet.contentSecurityPolicy({ directives: { ... } }));' }),
      ]),
      lesson('auth-security', '認証のセキュリティ', '認証関連の脆弱性と対策を学びます', [
        sec('よくある認証の脆弱性', '弱いパスワードポリシー、ブルートフォース攻撃への無防備、セッション固定攻撃、JWT の秘密鍵漏洩、OAuth のリダイレクト URI 検証不足。\n\n認証はアプリの「扉」です。ここが脆弱だとすべてのデータが危険にさらされます。'),
        sec('セッション固定攻撃', '攻撃者が自分のセッション ID を被害者に使わせ、ログイン後にそのセッションを乗っ取る攻撃です。ログイン成功時にセッション ID を再生成（regenerate）することで防御します。', { code: 'app.post("/login", async (req, res) => {\n  const user = await authenticate(req.body);\n  if (!user) return res.status(401).json({ error: "認証失敗" });\n  req.session.regenerate(() => {\n    req.session.userId = user.id;\n    res.json({ success: true });\n  });\n});' }),
        sec('レート制限と MFA', 'ログイン・パスワードリセットにレート制限を設け、ブルートフォースを防ぎます。重要なアカウントには多要素認証（MFA）を必須化します。\n\nパスワードリセットトークンは短い有効期限（15分程度）と1回限りの使用を設定します。'),
      ]),
      lesson('dependencies', '依存関係のセキュリティ', 'サプライチェーン攻撃と脆弱性管理を学びます', [
        sec('npm audit', '`npm audit` で依存パッケージの既知の脆弱性を検出します。`npm audit fix` で自動修正可能なものを更新します。\n\nCI で `npm audit --audit-level=high` を実行し、高 severity の脆弱性がある PR をブロックするのが一般的です。'),
        sec('Dependabot / Renovate', 'GitHub Dependabot や Renovate で依存パッケージの更新 PR を自動作成します。セキュリティパッチは早めにマージする文化を作りましょう。\n\nlockfile を Git 管理し、npm ci で再現性を確保することも重要です。'),
        sec('サプライチェーン攻撃', '悪意のあるパッケージや、正規パッケージの侵害（アカウント乗っ取り）が増えています。パッケージインストール前に名前とダウンロード数を確認し、不要な依存を減らすことが防御になります。', { tip: 'デモで npm audit の出力を確認し、脆弱性の severity と修正方法を読み解いてみてください。' }),
      ]),
      lesson('owasp-practice', '実践: セキュリティチェックリスト', 'リリース前のセキュリティ確認項目を学びます', [
        sec('入力検証', 'すべてのユーザー入力（フォーム、URL パラメータ、API ボディ、ファイルアップロード）をサーバー側で検証します。クライアント側のバリデーションは UX 用であり、セキュリティにはなりません。\n\n許可リスト（ホワイトリスト）方式で、許可された形式・値だけを受け入れます。'),
        sec('アクセス制御', 'すべての API エンドポイントで認証・認可をチェックします。「管理者 API に認証なし」は最も多い脆弱性の1つです。\n\n水平権限昇格（他人のデータにアクセス）と垂直権限昇格（一般ユーザーが管理者機能にアクセス）の両方を防ぎます。'),
        sec('ログとモニタリング', '認証失敗、権限エラー、異常なアクセスパターンをログに記録します。SIEM ツールやアラートでリアルタイム監視し、インシデントに迅速に対応できる体制を整えます。'),
      ]),
      lesson('next-steps', '次のステップ', 'セキュリティの学習を続けるための道筋を確認します', [
        sec('次に学ぶこと', '① 認証入門 — 安全な認証実装\n② HTTP 入門 — セキュリティヘッダーの詳細\n③ Docker 入門 — コンテナのセキュリティ\n④ Playwright 入門 — セキュリティテストの自動化'),
        sec('学習の道筋', 'セキュリティは一度学んで終わりではなく、新しい攻撃手法が常に登場します。OWASP Top 10 を定期的に確認し、自分のアプリを見直す習慣をつけましょう。\n\nおめでとうございます！Webセキュリティ入門をすべて学びました 🎉', { tip: '自分のアプリに securityheaders.com でヘッダーをチェックしてみてください。' }),
        sec('実践チェックリスト', '• すべてのユーザー入力をエスケープ/サニタイズしているか\n• SQL はパラメータ化クエリを使っているか\n• HTTPS と HSTS を設定しているか\n• CSP とセキュリティヘッダーを設定しているか\n• npm audit を CI で実行しているか'),
      ]),
    ],
  },
  {
    slug: 'seo',
    componentName: 'SeoTutorial',
    courseTitle: 'SEO',
    sidebarTitle: 'SEO チュートリアル',
    heroAccent: 'Search Engine Optimization',
    heroSub: 'メタデータ、構造化データ、Core Web Vitals、SSR の SEO まで。検索エンジンに評価されるサイトを10レッスンで学びます。',
    feature1: 'メタタグと構造化データのプレビューをデモで確認できます。',
    icon: '🔍',
    logoIcon: '🔍',
    section: 'practice',
    subtitle: '検索エンジン最適化',
    description: 'SEO の基本、メタデータ、構造化データ、パフォーマンス、SSR/SSG、サイトマップ、アクセシビリティとの関係まで。10レッスンで学びます。',
    gradient: 'linear-gradient(135deg, #4285f4, #34a853)',
    lessons: [
      lesson('intro', 'SEO とは？', '検索エンジン最適化の基本概念を理解します', [
        sec('SEO の目的', 'SEO（Search Engine Optimization）は、検索エンジン（Google 等）での表示順位を向上させ、オーガニック（自然）検索からの流入を増やす施策です。広告費をかけずに継続的なトラフィックを獲得できます。\n\nGoogle は200以上のランキング要因を考慮しますが、コンテンツの質、技術的な健全性、ユーザー体験が三大要素です。'),
        sec('クローラーとインデックス', 'Googlebot がページをクロール（巡回）し、内容をインデックス（データベースに登録）します。検索クエリに対して関連性と品質でランキングされます。\n\nrobots.txt でクロール制御、noindex でインデックス除外、sitemap.xml でクロールを促進します。', { tip: 'Google Search Console で自分のサイトのインデックス状況を確認してみてください。' }),
        sec('技術 SEO とコンテンツ SEO', '技術 SEO — サイトの構造、速度、モバイル対応、構造化データ。コンテンツ SEO — キーワード選定、見出し構造、内部リンク、コンテンツの質。\n\nフロントエンド開発者は技術 SEO が主戦場ですが、コンテンツ SEO との連携も重要です。'),
      ]),
      lesson('metadata', 'メタデータ', 'title、description、OGP タグの設定を学びます', [
        sec('title と description', 'title は検索結果のリンクテキスト、description はスニペット（説明文）に使われます。ページごとにユニークで、内容を正確に要約したものを設定します。', { code: '<head>\n  <title>React 入門チュートリアル | プログラミング学習</title>\n  <meta name="description" content="React の基本から Hooks、状態管理まで。初心者向けの10レッスンで学べます。" />\n</head>' }),
        sec('Open Graph と Twitter Card', 'SNS でシェアされた際のプレビュー画像・テキストを制御します。og:title、og:description、og:image、og:url が基本です。', { code: '<meta property="og:title" content="React 入門チュートリアル" />\n<meta property="og:description" content="初心者向け10レッスン" />\n<meta property="og:image" content="https://example.com/og-image.png" />\n<meta property="og:url" content="https://example.com/react" />\n<meta name="twitter:card" content="summary_large_image" />' }),
        sec('Next.js の Metadata API', 'Next.js 13+ の App Router では export const metadata で宣言的にメタデータを設定します。動的ページは generateMetadata 関数を使います。', { code: 'export const metadata = {\n  title: "React 入門",\n  description: "初心者向けチュートリアル",\n  openGraph: { images: ["/og-react.png"] },\n};' }),
      ]),
      lesson('structured-data', '構造化データ', 'JSON-LD でリッチリザルトを実現します', [
        sec('構造化データとは', 'Schema.org の語彙でページの内容を機械可読な形式で記述します。Google がリッチリザルト（星評価、パンくずリスト、FAQ 等）を検索結果に表示できるようになります。', { code: '<script type="application/ld+json">\n{\n  "@context": "https://schema.org",\n  "@type": "Article",\n  "headline": "React 入門チュートリアル",\n  "author": { "@type": "Person", "name": "太郎" },\n  "datePublished": "2024-01-15"\n}\n</script>' }),
        sec('主要なスキーマタイプ', 'Article（記事）、Product（商品）、FAQPage（FAQ）、BreadcrumbList（パンくず）、Organization（企業情報）、LocalBusiness（店舗情報）がよく使われます。\n\nGoogle の Rich Results Test で構造化データの検証ができます。'),
        sec('パンくずリスト', 'BreadcrumbList は検索結果にパンくずナビゲーションを表示します。サイト構造の理解を助け、クリック率が向上することがあります。', { tip: 'デモで Rich Results Test に URL を入力し、構造化データの解析結果を確認してみてください。' }),
      ]),
      lesson('performance', 'Core Web Vitals', 'ページ速度とユーザー体験指標を学びます', [
        sec('3つの Core Web Vitals', 'LCP（Largest Contentful Paint）— 最大コンテンツの表示速度。2.5秒以内が Good。FID/INP（Interactivity）— 操作への応答速度。CLS（Cumulative Layout Shift）— レイアウトの安定性。0.1 以下が Good。\n\nこれらは Google のランキング要因の1つです。'),
        sec('最適化手法', '画像の最適化（WebP、lazy loading、適切なサイズ）、フォントの最適化（font-display: swap、サブセット化）、JavaScript の削減（コード分割、Tree Shaking）、CDN の活用。\n\nNext.js の Image コンポーネントは自動で WebP 変換と lazy loading を行います。', { code: 'import Image from "next/image";\n<Image src="/hero.jpg" alt="ヒーロー画像" width={1200} height={600} priority />' }),
        sec('測定ツール', 'Lighthouse（Chrome DevTools）、PageSpeed Insights、Web Vitals ライブラリで測定します。本番環境の実ユーザーデータは Chrome UX Report（CrUX）で確認できます。'),
      ]),
      lesson('ssr-seo', 'SSR/SSG と SEO', 'レンダリング方式の SEO への影響を学びます', [
        sec('CSR の SEO 問題', 'クライアントサイドレンダリング（CSR）のみの SPA は、初回 HTML が空の <div id="root"> だけです。クローラーが JavaScript 実行後のコンテンツを取得できない場合、インデックスされません。\n\nGoogle は JavaScript を実行しますが、レンダリングに時間がかかり、完全にインデックスされないリスクがあります。'),
        sec('SSR と SSG', 'SSR（Server-Side Rendering）— リクエスト時に HTML を生成。SSG（Static Site Generation）— ビルド時に HTML を生成。どちらも完全な HTML がクローラーに届きます。\n\nNext.js ではページごとに export const dynamic や generateStaticParams でレンダリング方式を選択できます。', { code: '// SSG（デフォルト）\nexport default function Page() { return <h1>静的ページ</h1>; }\n\n// SSR\nexport const dynamic = "force-dynamic";\n\n// ISR（Incremental Static Regeneration）\nexport const revalidate = 3600; // 1時間ごとに再生成' }),
        sec('ハイブリッドアプローチ', '公開ページ（ブログ、ランディング）は SSG/SSR、ユーザー固有ページ（ダッシュボード）は CSR という使い分けが一般的です。SEO が必要なページだけサーバーレンダリングします。'),
      ]),
      lesson('sitemap', 'サイトマップと robots.txt', 'クローラーの誘導を学びます', [
        sec('sitemap.xml', 'サイト内の全 URL をリストし、クローラーに発見を促します。更新頻度（changefreq）と優先度（priority）も指定できます。', { code: '<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n  <url>\n    <loc>https://example.com/react</loc>\n    <lastmod>2024-01-15</lastmod>\n    <changefreq>weekly</changefreq>\n    <priority>0.8</priority>\n  </url>\n</urlset>' }),
        sec('Next.js でのサイトマップ', 'App Router では app/sitemap.ts で動的にサイトマップを生成します。', { code: 'export default function sitemap() {\n  return [\n    { url: "https://example.com", lastModified: new Date() },\n    { url: "https://example.com/react", lastModified: new Date() },\n  ];\n}' }),
        sec('robots.txt', 'クローラーのアクセス制御ファイルです。Disallow で特定パスをブロック、Sitemap でサイトマップの場所を指定します。', { code: 'User-agent: *\nAllow: /\nDisallow: /api/\nDisallow: /admin/\nSitemap: https://example.com/sitemap.xml' }),
      ]),
      lesson('semantic-html', 'セマンティック HTML', 'HTML 構造と SEO の関係を学びます', [
        sec('見出しの階層', 'h1 はページに1つだけ。h2 → h3 の順序を守り、見出しを飛ばさない（h1 の次に h3 は NG）。見出しはコンテンツの構造を表し、検索エンジンがページの主題を理解する手がかりになります。'),
        sec('セマンティック要素', 'header、nav、main、article、section、aside、footer を適切に使い、ページ構造を明確にします。div のみの構造より、検索エンジンとアクセシビリティツールの両方に有益です。\n\nmain 要素はページの主要コンテンツを1つだけ囲みます。'),
        sec('alt 属性とリンクテキスト', 'img の alt 属性は画像の内容を説明し、画像検索とアクセシビリティに影響します。a タグのリンクテキストは「こちらをクリック」ではなく、リンク先の内容を説明するテキストを使います。', { tip: 'デモでページの見出し構造をアウトライン表示し、階層が正しいか確認してみてください。' }),
      ]),
      lesson('canonical', 'canonical URL と重複コンテンツ', 'URL の正規化を学びます', [
        sec('重複コンテンツの問題', '同じコンテンツが複数 URL でアクセス可能だと、検索エンジンがどれをインデックスすべきか迷い、ランキングが分散します。例: /page と /page/ や ?sort=date と ?sort=name。'),
        sec('canonical タグ', 'link rel="canonical" で正規 URL を指定します。検索エンジンに「この URL が正式版です」と伝えます。', { code: '<link rel="canonical" href="https://example.com/react" />\n\n// Next.js\nexport const metadata = {\n  alternates: { canonical: "https://example.com/react" },\n};' }),
        sec('hreflang', '多言語サイトでは hreflang 属性で言語・地域バージョンを指定します。Google が適切な言語版を検索結果に表示します。', { code: '<link rel="alternate" hreflang="ja" href="https://example.com/ja/react" />\n<link rel="alternate" hreflang="en" href="https://example.com/en/react" />\n<link rel="alternate" hreflang="x-default" href="https://example.com/react" />' }),
      ]),
      lesson('tools', 'SEO ツール', '分析・監視ツールの活用を学びます', [
        sec('Google Search Console', 'インデックス状況、検索クエリ、クリック数、クロールエラーを確認できます。サイトマップの送信、URL のインデックス登録リクエストも可能です。\n\n新サイトは Search Console に登録し、サイトマップを送信するのが最初のステップです。'),
        sec('Google Analytics', 'ユーザー行為（ページビュー、滞在時間、離脱率）を分析します。SEO 施策の効果測定（オーガニック検索からの流入増加）に使います。\n\nGA4 はイベントベースの新しい分析モデルです。Search Console と連携すると検索クエリとユーザー行動を統合分析できます。'),
        sec('その他のツール', 'Ahrefs / SEMrush — キーワード調査、被リンク分析。Screaming Frog — サイトのクロールと技術 SEO 監査。Lighthouse — パフォーマンスと SEO スコアの測定。'),
      ]),
      lesson('next-steps', '次のステップ', 'SEO の学習を続けるための道筋を確認します', [
        sec('次に学ぶこと', '① i18n 入門 — 多言語 SEO\n② CSSアニメーション — CLS 対策\n③ HTTP 入門 — キャッシュと SEO\n④ アクセシビリティ — SEO との相乗効果'),
        sec('学習の道筋', 'SEO は技術とコンテンツの両面から取り組む継続的な施策です。メタデータと構造化データを正しく設定し、Core Web Vitals を改善し、Search Console で監視——このサイクルを回しましょう。\n\nおめでとうございます！SEO 入門をすべて学びました 🎉', { tip: '自分のサイトを Lighthouse の SEO カテゴリでスコアリングしてみてください。' }),
        sec('実践チェックリスト', '• 全ページにユニークな title/description があるか\n• 構造化データ（JSON-LD）を設定しているか\n• sitemap.xml と robots.txt を配置しているか\n• Core Web Vitals が Good 範囲か\n• Search Console に登録しているか'),
      ]),
    ],
  },
  {
    slug: 'docker',
    componentName: 'DockerTutorial',
    courseTitle: 'Docker',
    sidebarTitle: 'Docker チュートリアル',
    heroAccent: 'Docker Containers',
    heroSub: 'イメージ、コンテナ、Dockerfile、Compose まで。開発環境の統一とデプロイを10レッスンで学びます。',
    feature1: 'コンテナの起動・停止とログ確認をデモで体験できます。',
    icon: '🐳',
    logoIcon: '🐳',
    section: 'practice',
    subtitle: 'コンテナ技術',
    description: 'Docker の基本概念、イメージ、コンテナ、Dockerfile、Docker Compose、ボリューム、ネットワーク、本番デプロイまで。10レッスンで学びます。',
    gradient: 'linear-gradient(135deg, #2496ed, #1a6fb5)',
    lessons: [
      lesson('intro', 'Docker とは？', 'コンテナ技術の概念と仮想化との違いを理解します', [
        sec('コンテナとは', 'コンテナはアプリケーションとその依存関係（ランタイム、ライブラリ、設定）をパッケージ化した軽量な実行単位です。「どのマシンでも同じように動く」環境を提供します。\n\n「私のマシンでは動くのに」問題を解決し、開発・テスト・本番で同一の環境を保証します。'),
        sec('VM との違い', '仮想マシン（VM）は OS 全体を仮想化し、数 GB のディスクと数分の起動時間が必要です。コンテナはホスト OS のカーネルを共有し、数 MB〜数百 MB、数秒で起動します。\n\nVM は強い隔離が必要な場合、コンテナはアプリの配布・実行が目的の場合に適しています。', { tip: 'デモで docker run hello-world を実行し、コンテナが起動して終了する流れを確認してみてください。' }),
        sec('Docker の構成要素', 'Docker Engine（実行環境）、Docker Image（テンプレート）、Docker Container（実行インスタンス）、Dockerfile（イメージの設計図）、Docker Hub（イメージレジストリ）、Docker Compose（複数コンテナの管理）。'),
      ]),
      lesson('images', 'イメージ', 'Docker イメージの概念と操作を学びます', [
        sec('イメージの構造', 'イメージはレイヤー（層）の積み重ねです。各レイヤーは変更の差分を記録し、共通レイヤーは再利用されるため、ディスク効率が良いです。\n\nベースイメージ（node:20-alpine）にアプリケーションコードを追加するレイヤーを重ねて独自イメージを作ります。'),
        sec('イメージの操作', 'docker pull でレジストリから取得、docker images で一覧、docker rmi で削除します。', { code: 'docker pull node:20-alpine\ndocker images\ndocker rmi node:20-alpine\ndocker image prune  # 未使用イメージを一括削除' }),
        sec('タグとバージョン', 'イメージ名:タグ の形式です。node:20-alpine は Node.js 20 の Alpine Linux 版。latest タグは避け、具体的なバージョンを指定するのが本番のベストプラクティスです。\n\nalpine は最小限の Linux ディストリビューションで、イメージサイズを大幅に削減できます。'),
      ]),
      lesson('containers', 'コンテナ', 'コンテナのライフサイクル管理を学びます', [
        sec('基本操作', 'docker run でコンテナを起動、docker ps で一覧、docker stop/start で停止・再開、docker rm で削除します。', { code: 'docker run -d --name my-nginx -p 8080:80 nginx\ndocker ps\ndocker logs my-nginx\ndocker stop my-nginx\ndocker rm my-nginx' }),
        sec('ポートマッピング', '-p ホストポート:コンテナポート でポートを公開します。-p 8080:80 はホストの 8080 をコンテナの 80 にマッピングします。\n\n-d はバックグラウンド実行、--name はコンテナに名前を付け、-it はインタラクティブモード（シェル接続用）です。'),
        sec('コンテナ内の操作', 'docker exec -it コンテナ名 sh でコンテナ内のシェルに入ります。デバッグやログ確認に使います。\n\ndocker logs -f でリアルタイムログを追跡できます。', { tip: 'デモで nginx コンテナを起動し、localhost:8080 でアクセスしてみてください。' }),
      ]),
      lesson('dockerfile', 'Dockerfile', '独自イメージの作成を学びます', [
        sec('Dockerfile の基本', 'FROM（ベースイメージ）、WORKDIR（作業ディレクトリ）、COPY（ファイルコピー）、RUN（コマンド実行）、EXPOSE（ポート公開）、CMD（起動コマンド）が基本指令です。', { code: 'FROM node:20-alpine\nWORKDIR /app\nCOPY package*.json ./\nRUN npm ci --omit=dev\nCOPY . .\nEXPOSE 3000\nCMD ["node", "index.js"]' }),
        sec('レイヤーキャッシュ', 'Dockerfile の各命令はレイヤーを作成します。変更のないレイヤーはキャッシュが使われ、ビルドが高速化されます。\n\npackage.json の変更が少ない場合、COPY package*.json → RUN npm ci を先に書くことで、ソースコード変更時に npm ci をスキップできます。'),
        sec('マルチステージビルド', 'ビルド用ステージと実行用ステージを分離し、最終イメージにビルドツールを含めません。イメージサイズを大幅に削減できます。', { code: 'FROM node:20-alpine AS builder\nWORKDIR /app\nCOPY . .\nRUN npm ci && npm run build\n\nFROM node:20-alpine\nWORKDIR /app\nCOPY --from=builder /app/dist ./dist\nCOPY package*.json ./\nRUN npm ci --omit=dev\nCMD ["node", "dist/index.js"]' }),
      ]),
      lesson('compose', 'Docker Compose', '複数コンテナの管理を学びます', [
        sec('Compose の役割', 'Web アプリ + DB + Redis など複数コンテナを docker-compose.yml で定義し、1コマンドで起動・停止します。開発環境の構築が劇的に簡単になります。', { code: '# docker-compose.yml\nservices:\n  app:\n    build: .\n    ports: ["3000:3000"]\n    depends_on: [db]\n    environment:\n      DATABASE_URL: postgresql://user:pass@db:5432/mydb\n  db:\n    image: postgres:16-alpine\n    environment:\n      POSTGRES_USER: user\n      POSTGRES_PASSWORD: pass\n      POSTGRES_DB: mydb\n    volumes: [pgdata:/var/lib/postgresql/data]\nvolumes:\n  pgdata:' }),
        sec('Compose コマンド', 'docker compose up -d で起動、docker compose down で停止・削除、docker compose logs -f でログ確認、docker compose exec app sh でシェル接続。', { code: 'docker compose up -d\ndocker compose ps\ndocker compose logs -f app\ndocker compose down -v  # ボリュームも削除' }),
        sec('開発ワークフロー', 'ソースコードをボリュームマウントし、ホストの変更がコンテナに即座に反映される開発構成が一般的です。', { code: 'services:\n  app:\n    build: .\n    volumes: ["./src:/app/src"]\n    command: npm run dev' }),
      ]),
      lesson('volumes', 'ボリュームとネットワーク', 'データ永続化とコンテナ間通信を学びます', [
        sec('ボリューム', 'コンテナは削除されるとデータも消えます。ボリュームでデータを永続化します。名前付きボリューム（Docker 管理）とバインドマウント（ホストパス指定）の2種類があります。', { code: '# 名前付きボリューム\ndocker volume create pgdata\ndocker run -v pgdata:/var/lib/postgresql/data postgres\n\n# バインドマウント\ndocker run -v $(pwd)/data:/app/data my-app' }),
        sec('Docker ネットワーク', 'Compose ではサービス名がホスト名として使えます（db:5432 で PostgreSQL に接続）。デフォルトの bridge ネットワークでコンテナ間通信が可能です。\n\ncustom ネットワークを作成し、特定のコンテナだけを接続するセグメント分離も可能です。'),
        sec('.dockerignore', '.gitignore と同様、Docker ビルド時に除外するファイルを指定します。node_modules、.git、.env を除外し、ビルド速度とイメージサイズを改善します。'),
      ]),
      lesson('production', '本番デプロイ', 'コンテナの本番運用を学びます', [
        sec('本番イメージのベストプラクティス', '①非 root ユーザーで実行 ②alpine ベースで軽量化 ③マルチステージビルド ④ヘルスチェック設定 ⑤.secrets をイメージに含めない。\n\nイメージサイズが小さいほど、デプロイが速く、攻撃面も小さくなります。', { code: 'HEALTHCHECK --interval=30s --timeout=3s CMD curl -f http://localhost:3000/health || exit 1\n\nUSER node' }),
        sec('コンテナオーケストレーション', '本番では Docker Compose 単体ではなく、Kubernetes（K8s）、AWS ECS、Google Cloud Run などのオーケストレーターで管理します。スケーリング、ローリングアップデート、自己修復が自動化されます。'),
        sec('CI/CD 統合', 'GitHub Actions で Docker イメージをビルドし、レジストリ（Docker Hub、ECR、GCR）にプッシュ、本番環境にデプロイするパイプラインが標準的です。', { code: '# GitHub Actions\n- uses: docker/build-push-action@v5\n  with:\n    push: true\n    tags: myregistry/myapp:${{ github.sha }}' }),
      ]),
      lesson('debugging', 'デバッグとトラブルシューティング', 'コンテナの問題解決を学びます', [
        sec('ログと inspect', 'docker logs でアプリログ、docker inspect でコンテナの詳細設定（環境変数、マウント、ネットワーク）を確認します。', { code: 'docker logs --tail 100 -f my-app\ndocker inspect my-app | jq ".[0].Config.Env"\ndocker stats  # リソース使用量' }),
        sec('よくある問題', 'ポート競合（Address already in use）→ 別ポートを使うか既存プロセスを停止。権限エラー → 非 root ユーザーとファイル権限を確認。ネットワーク接続失敗 → サービス名とポート、depends_on の順序を確認。\n\nコンテナ内から curl や ping で接続テストを行います。'),
        sec('ビルドのデバッグ', 'docker build --no-cache でキャッシュを無視して再ビルド。--progress=plain で詳細なビルドログを表示します。', { tip: 'デモで意図的に壊れた Dockerfile をビルドし、エラーメッセージを読み解いてみてください。' }),
      ]),
      lesson('dev-workflow', '開発ワークフロー', 'Docker を日常開発に組み込む方法を学びます', [
        sec('devcontainer', 'VS Code の Dev Containers 機能で、プロジェクトの Docker 設定から開発環境を自動構築します。.devcontainer/devcontainer.json で設定し、チーム全員が同一環境で開発できます。'),
        sec('DB のコンテナ化', 'PostgreSQL、Redis、MongoDB を Compose で起動し、ローカル開発の DB として使います。本番と同じ DB エンジンで開発できるのが最大のメリットです。\n\nデータは名前付きボリュームで永続化し、docker compose down -v で初期化できます。'),
        sec('ホットリロード', 'ソースコードをバインドマウントし、nodemon や vite の --watch でファイル変更時に自動再起動します。コンテナを再ビルドせずに開発を続けられます。'),
      ]),
      lesson('next-steps', '次のステップ', 'Docker の学習を続けるための道筋を確認します', [
        sec('次に学ぶこと', '① Supabase 入門 — ローカル Supabase スタック\n② Prisma 入門 — コンテナ DB との連携\n③ Webセキュリティ — コンテナのセキュリティ\n④ CI/CD — Docker イメージの自動ビルド'),
        sec('学習の道筋', 'Docker は現代の開発・デプロイの基盤技術です。Dockerfile を書き、Compose で複数サービスを管理し、CI/CD で自動デプロイ——この流れをマスターすれば、どんな環境でも自信を持って開発できます。\n\nおめでとうございます！Docker 入門をすべて学びました 🎉', { tip: '自分のプロジェクトに docker-compose.yml を追加し、DB をコンテナ化してみてください。' }),
        sec('実践チェックリスト', '• Dockerfile でマルチステージビルドを使っているか\n• .dockerignore を設定しているか\n• 本番イメージは非 root ユーザーで実行しているか\n• docker compose で開発環境を統一しているか\n• シークレットをイメージに含めていないか'),
      ]),
    ],
  },
  {
    slug: 'websocket',
    componentName: 'WebsocketTutorial',
    courseTitle: 'WebSocket',
    sidebarTitle: 'WebSocket チュートリアル',
    heroAccent: 'WebSocket / Realtime',
    heroSub: 'WebSocket プロトコル、Socket.IO、リアルタイム通信パターンまで。双方向通信を10レッスンで学びます。',
    feature1: 'リアルタイムメッセージの送受信をデモで体験できます。',
    icon: '🔌',
    logoIcon: '🔌',
    section: 'practice',
    subtitle: 'リアルタイム通信',
    description: 'WebSocket の基本、ネイティブ API、Socket.IO、リアルタイムパターン、スケーリング、セキュリティ、React 統合まで。10レッスンで学びます。',
    gradient: 'linear-gradient(135deg, #8b5cf6, #6d28d9)',
    lessons: [
      lesson('intro', 'WebSocket とは？', 'リアルタイム通信の必要性と WebSocket の位置づけを理解します', [
        sec('HTTP の限界', 'HTTP はリクエスト・レスポンスモデルで、サーバーからクライアントへのプッシュができません。リアルタイム更新にはポーリング（定期的な GET）やロングポーリング（接続を長く保持）が使われていましたが、非効率です。\n\nチャット、株価ティッカー、共同編集、ゲームなど「即座にデータを送受信」したい場面では WebSocket が最適です。'),
        sec('WebSocket の特徴', 'TCP 上の双方向通信プロトコルです。HTTP でハンドシェイク後、接続を WebSocket にアップグレードし、以降はフルデュプlex 通信が可能です。ヘッダーオーバーヘッドが小さく、低レイテンシです。\n\nws:// または wss://（暗号化）の URL で接続します。'),
        sec('ユースケース', '• チャットアプリ • リアルタイム通知 • 共同編集 • ライブスコア • IoT データ配信 • オンラインゲーム\n\nSupabase Realtime、Socket.IO、Pusher などのライブラリ/サービスが WebSocket を抽象化しています。', { tip: 'デモで WebSocket 接続のハンドシェイクを Network タブで確認してみてください。' }),
      ]),
      lesson('protocol', 'WebSocket プロトコル', 'ハンドシェイクとメッセージフレームを学びます', [
        sec('ハンドシェイク', 'クライアントが HTTP Upgrade リクエストを送り、サーバーが 101 Switching Protocols で応答します。Sec-WebSocket-Key と Sec-WebSocket-Accept で接続を確認します。\n\n通常の HTTP リクエストと同じポート（80/443）を使うため、ファイアウォールの設定変更が不要です。'),
        sec('メッセージフレーム', 'テキストフレーム（UTF-8 文字列）とバイナリフレーム（画像、音声等）を送受信できます。ping/pong フレームで接続の生存確認（ハートビート）を行います。\n\n接続が切れた場合、クライアントは自動再接続（exponential backoff）を実装するのが一般的です。'),
        sec('HTTP との共存', '同じサーバーで HTTP と WebSocket を共存させます。Express では HTTP サーバーを作成し、WebSocket サーバーをアタッチするパターンが一般的です。'),
      ]),
      lesson('native-api', 'ネイティブ WebSocket API', 'ブラウザと Node.js の WebSocket API を学びます', [
        sec('ブラウザ側', 'new WebSocket(url) で接続。onopen、onmessage、onclose、onerror イベントで状態を監視します。', { code: 'const ws = new WebSocket("wss://example.com/ws");\n\nws.onopen = () => {\n  console.log("接続完了");\n  ws.send(JSON.stringify({ type: "join", room: "general" }));\n};\n\nws.onmessage = (event) => {\n  const data = JSON.parse(event.data);\n  console.log("受信:", data);\n};\n\nws.onclose = () => console.log("切断");\nws.send("Hello!");' }),
        sec('Node.js 側（ws パッケージ）', 'ws パッケージで WebSocket サーバーを構築します。', { code: 'import { WebSocketServer } from "ws";\n\nconst wss = new WebSocketServer({ port: 8080 });\n\nwss.on("connection", (ws) => {\n  ws.on("message", (data) => {\n    const msg = JSON.parse(data);\n    wss.clients.forEach(client => {\n      if (client.readyState === ws.OPEN) {\n        client.send(JSON.stringify({ type: "broadcast", data: msg }));\n      }\n    });\n  });\n});' }),
        sec('メッセージ形式', 'JSON が最も一般的です。type フィールドでメッセージ種別を区別し、ルーティングします。{ type: "chat", payload: { text: "Hello" } } のような構造が定番です。'),
      ]),
      lesson('socket-io', 'Socket.IO', '高機能なリアルタイムライブラリを学びます', [
        sec('Socket.IO の特徴', 'WebSocket の上に自動再接続、ルーム、名前空間、フォールバック（ロングポーリング）を提供します。本番のリアルタイムアプリでは Socket.IO が最も広く使われています。', { code: '// サーバー\nimport { Server } from "socket.io";\nconst io = new Server(httpServer, { cors: { origin: "*" } });\n\nio.on("connection", (socket) => {\n  socket.join("room-1");\n  socket.on("chat", (msg) => io.to("room-1").emit("chat", msg));\n  socket.on("disconnect", () => console.log("切断"));\n});' }),
        sec('クライアント', 'socket.io-client で接続し、emit/on でイベントベースの通信を行います。', { code: 'import { io } from "socket.io-client";\nconst socket = io("http://localhost:3000");\n\nsocket.on("connect", () => console.log("接続:", socket.id));\nsocket.emit("chat", { text: "Hello!" });\nsocket.on("chat", (msg) => console.log("受信:", msg));' }),
        sec('ルームと名前空間', 'socket.join(room) でルームに参加し、io.to(room).emit() でルーム内のクライアントだけに送信します。名前空間（io.of("/admin")）で機能を分離できます。\n\nチャットの部屋分け、ゲームのマッチングルームなどに使います。'),
      ]),
      lesson('patterns', 'リアルタイムパターン', 'チャット、通知、共同編集の実装パターンを学びます', [
        sec('チャットアプリ', 'メッセージ送信 → サーバーが DB に保存 → ルーム内の全クライアントに配信。履歴は接続時に DB から取得して表示します。\n\nメッセージ ID とタイムスタンプで順序を保証し、重複排除します。'),
        sec('リアルタイム通知', 'ユーザーごとの通知チャンネル（user:{id}）を作成し、イベント発生時にそのチャンネルにプッシュします。未読カウントの更新もリアルタイムで配信します。', { code: 'socket.on("connection", (socket) => {\n  const userId = authenticate(socket);\n  socket.join(`user:${userId}`);\n});\n\nfunction notifyUser(userId, notification) {\n  io.to(`user:${userId}`).emit("notification", notification);\n}' }),
        sec('Presence（オンライン状態）', '接続・切断時にユーザーのオンライン状態を更新し、ルーム内のメンバーに配信します。Socket.IO の socket.data でユーザー情報を保持します。', { tip: 'デモで2つのブラウザタブを開き、チャットメッセージがリアルタイムで同期されることを確認してみてください。' }),
      ]),
      lesson('scaling', 'スケーリング', '複数サーバーでの WebSocket 運用を学びます', [
        sec('スティッキーセッションの問題', 'WebSocket は接続を維持するため、ロードバランサーで複数サーバーに分散すると、同じユーザーの接続が異なるサーバーに行く可能性があります。ルーム内の配信が届かない問題が起きます。'),
        sec('Redis Pub/Sub', 'Socket.IO の Redis Adapter で、サーバー間のメッセージを Redis Pub/Sub で中継します。どのサーバーに接続していても、全クライアントにメッセージが届きます。', { code: 'import { createAdapter } from "@socket.io/redis-adapter";\nimport { createClient } from "redis";\n\nconst pubClient = createClient({ url: "redis://localhost:6379" });\nconst subClient = pubClient.duplicate();\nio.adapter(createAdapter(pubClient, subClient));' }),
        sec('接続数の管理', '1サーバーあたり数万の WebSocket 接続が可能ですが、メモリと CPU の監視が重要です。不要な接続はタイムアウトで切断し、ping/pong で死活監視します。'),
      ]),
      lesson('react-integration', 'React との統合', 'React アプリで WebSocket を使う方法を学びます', [
        sec('カスタムフック', 'useWebSocket フックで接続管理をカプセル化します。コンポーネントのマウント時に接続、アンマウント時に切断します。', { code: 'function useWebSocket(url) {\n  const [messages, setMessages] = useState([]);\n  const wsRef = useRef(null);\n\n  useEffect(() => {\n    const ws = new WebSocket(url);\n    ws.onmessage = (e) => setMessages(prev => [...prev, JSON.parse(e.data)]);\n    wsRef.current = ws;\n    return () => ws.close();\n  }, [url]);\n\n  const send = (data) => wsRef.current?.send(JSON.stringify(data));\n  return { messages, send };\n}' }),
        sec('状態管理', '受信メッセージは useState や Zustand で管理します。大量のメッセージでは仮想スクロール（react-virtuoso 等）で DOM ノード数を抑制します。\n\n接続状態（connecting、connected、disconnected）を UI に表示し、再接続中はインジケーターを出します。'),
        sec('Socket.IO + React', 'socket.io-client のシングルトンインスタンスを Context で提供し、コンポーネントから useSocket() でアクセスするパターンが一般的です。'),
      ]),
      lesson('security', 'WebSocket のセキュリティ', '認証と入力検証を学びます', [
        sec('接続時の認証', 'WebSocket のハンドシェイク時に Cookie やトークンで認証します。未認証の接続は即座に切断します。', { code: 'io.use((socket, next) => {\n  const token = socket.handshake.auth.token;\n  try {\n    socket.data.user = jwt.verify(token, SECRET);\n    next();\n  } catch {\n    next(new Error("認証失敗"));\n  }\n});' }),
        sec('入力検証', '受信メッセージの type と payload を検証します。Zod スキーマでメッセージ形式を定義し、不正なメッセージは無視またはエラーを返します。\n\nメッセージサイズの上限を設定し、大量送信（スパム）を防ぎます。'),
        sec('レート制限', '接続ごと・ユーザーごとにメッセージ送信レートを制限します。超過時は警告を送り、繰り返す場合は切断します。'),
      ]),
      lesson('testing', 'テスト', 'WebSocket 機能のテスト方法を学びます', [
        sec('サーバーのテスト', 'socket.io-client のテスト用インスタンスでサーバーに接続し、emit → on で送受信を検証します。', { code: 'import { io as Client } from "socket.io-client";\n\ntest("chat message broadcast", async () => {\n  const client = Client(`http://localhost:${port}`);\n  await new Promise(r => client.on("connect", r));\n  client.emit("chat", { text: "test" });\n  const msg = await new Promise(r => client.on("chat", r));\n  expect(msg.text).toBe("test");\n  client.disconnect();\n});' }),
        sec('E2E テスト', 'Playwright で WebSocket 通信を含む UI をテストします。メッセージ送信 → 画面に表示されることを検証します。'),
        sec('負荷テスト', 'artillery や k6 で大量の WebSocket 接続をシミュレートし、サーバーのスループットと安定性を検証します。'),
      ]),
      lesson('next-steps', '次のステップ', 'WebSocket の学習を続けるための道筋を確認します', [
        sec('次に学ぶこと', '① Supabase 入門 — Realtime 機能\n② GraphQL 入門 — Subscription\n③ 認証入門 — WebSocket 認証\n④ Docker 入門 — WebSocket サーバーのコンテナ化'),
        sec('学習の道筋', 'WebSocket はリアルタイム体験を実現する核心技術です。ネイティブ API で仕組みを理解し、Socket.IO で実用的なアプリを構築し、Redis でスケール——段階的に学びましょう。\n\nおめでとうございます！WebSocket 入門をすべて学びました 🎉', { tip: 'シンプルなチャットアプリを Socket.IO で作り、複数タブでリアルタイム通信を試してみてください。' }),
        sec('実践チェックリスト', '• 接続時に認証を行っているか\n• 自動再接続を実装しているか\n• メッセージ形式を定義・検証しているか\n• スケール時に Redis Adapter を使っているか\n• レート制限を設けているか'),
      ]),
    ],
  },
  {
    slug: 'i18n',
    componentName: 'I18nTutorial',
    courseTitle: 'i18n',
    sidebarTitle: 'i18n チュートリアル',
    heroAccent: 'Internationalization',
    heroSub: 'next-intl、react-i18next、多言語ルーティング、複数形・日付フォーマットまで。国際化を10レッスンで学びます。',
    feature1: '言語切り替えと翻訳プレビューをデモで体験できます。',
    icon: '🌍',
    logoIcon: '🌍',
    section: 'practice',
    subtitle: '国際化と多言語対応',
    description: 'i18n の基本概念、next-intl、react-i18next、翻訳ファイル管理、ルーティング、複数形、RTL 対応、テストまで。10レッスンで学びます。',
    gradient: 'linear-gradient(135deg, #0ea5e9, #0284c7)',
    lessons: [
      lesson('intro', 'i18n とは？', '国際化とローカライゼーションの概念を理解します', [
        sec('i18n と l10n', 'i18n（internationalization）はアプリを多言語対応可能にする設計・実装。l10n（localization）は特定の言語・地域向けに翻訳・調整することです。i18n が土台、l10n が仕上げです。\n\n「i18n」は i と n の間に18文字——internationalization の略です。'),
        sec('なぜ i18n が必要か', 'グローバルなユーザーにサービスを提供するには多言語対応が必須です。日本語のみのサイトは世界人口の約2%にしか届きません。UI テキスト、エラーメッセージ、日付・通貨の表示形式すべてが対象です。\n\nSEO でも多言語サイトは hreflang で各言語版を正しくインデックスできます。', { tip: 'デモで言語を切り替え、UI テキストと日付フォーマットが変わることを確認してみてください。' }),
        sec('i18n の範囲', '• UI テキストの翻訳 • 日付・時刻・数値のフォーマット • 通貨表示 • 複数形の処理 • RTL（右から左）レイアウト • 画像・メタデータの多言語化'),
      ]),
      lesson('basics', '基本的な実装', '翻訳キーと翻訳ファイルの管理を学びます', [
        sec('翻訳キー', 'UI テキストをハードコードせず、翻訳キーで参照します。t("welcome.message") でキーに対応する翻訳テキストを取得します。', { code: '// locales/ja.json\n{\n  "welcome": {\n    "message": "ようこそ",\n    "description": "アプリへようこそ"\n  },\n  "button": {\n    "submit": "送信",\n    "cancel": "キャンセル"\n  }\n}\n\n// コンポーネント\n<p>{t("welcome.message")}</p>' }),
        sec('名前空間', '大規模アプリでは翻訳を機能ごとにファイル分割します。common.json、auth.json、dashboard.json のように名前空間で管理します。\n\nキーの衝突を防ぎ、翻訳ファイルの保守性が向上します。'),
        sec('ネストとパラメータ', '翻訳テキストに動的な値を埋め込めます。{{name}} プレースホルダーでユーザー名などを挿入します。', { code: '// locales/ja.json\n{ "greeting": "こんにちは、{{name}}さん" }\n\n// 使用\nt("greeting", { name: "太郎" })  // → "こんにちは、太郎さん"' }),
      ]),
      lesson('next-intl', 'next-intl', 'Next.js App Router 向け i18n を学びます', [
        sec('セットアップ', 'next-intl は Next.js App Router に最適化された i18n ライブラリです。ミドルウェアでロケール検出、Server Components で翻訳取得が可能です。', { code: 'npm install next-intl\n\n// middleware.ts\nimport createMiddleware from "next-intl/middleware";\nexport default createMiddleware({ locales: ["ja", "en"], defaultLocale: "ja" });\nexport const config = { matcher: ["/((?!api|_next|.*\\\\..*).*)"] };' }),
        sec('App Router 構成', 'app/[locale]/layout.tsx でロケールを受け取り、NextIntlClientProvider で翻訳を提供します。', { code: '// app/[locale]/layout.tsx\nimport { NextIntlClientProvider } from "next-intl";\nimport { getMessages } from "next-intl/server";\n\nexport default async function Layout({ children, params: { locale } }) {\n  const messages = await getMessages();\n  return (\n    <html lang={locale}>\n      <body>\n        <NextIntlClientProvider messages={messages}>{children}</NextIntlClientProvider>\n      </body>\n    </html>\n  );\n}' }),
        sec('Server Components', 'getTranslations() で Server Component 内の翻訳を取得します。クライアントに翻訳データを送る量を最小化できます。', { code: 'import { getTranslations } from "next-intl/server";\n\nexport default async function Page() {\n  const t = await getTranslations("HomePage");\n  return <h1>{t("title")}</h1>;\n}' }),
      ]),
      lesson('react-i18next', 'react-i18next', 'React 向け i18n ライブラリを学びます', [
        sec('セットアップ', 'react-i18next は React で最も広く使われる i18n ライブラリです。i18next がコアで、React バインディングを提供します。', { code: 'import i18n from "i18next";\nimport { initReactI18next } from "react-i18next";\n\ni18n.use(initReactI18next).init({\n  resources: {\n    ja: { translation: jaTranslations },\n    en: { translation: enTranslations },\n  },\n  lng: "ja",\n  fallbackLng: "en",\n});' }),
        sec('useTranslation フック', 'useTranslation() で t 関数と i18n インスタンスを取得します。', { code: 'import { useTranslation } from "react-i18next";\n\nfunction Welcome() {\n  const { t, i18n } = useTranslation();\n  return (\n    <div>\n      <h1>{t("welcome.message")}</h1>\n      <button onClick={() => i18n.changeLanguage("en")}>English</button>\n    </div>\n  );\n}' }),
        sec('Trans コンポーネント', '翻訳テキスト内に HTML や React コンポーネントを埋め込む場合に Trans コンポーネントを使います。', { code: '<Trans i18nKey="terms">\n  利用規約に<Link to="/terms">同意</Link>します\n</Trans>' }),
      ]),
      lesson('formatting', '日付・数値・通貨', 'ロケール依存のフォーマットを学びます', [
        sec('Intl API', 'ブラウザ標準の Intl API で日付、数値、通貨をロケールに応じてフォーマットします。', { code: 'new Intl.DateTimeFormat("ja-JP", { dateStyle: "long" }).format(new Date());\n// → "2024年1月15日"\n\nnew Intl.NumberFormat("ja-JP", { style: "currency", currency: "JPY" }).format(1500);\n// → "￥1,500"\n\nnew Intl.RelativeTimeFormat("ja", { numeric: "auto" }).format(-1, "day");\n// → "昨日"' }),
        sec('i18n ライブラリのフォーマット', 'next-intl や react-i18next でもフォーマット関数を提供します。useFormatter() や i18n.formatDate() で統一的にフォーマットできます。'),
        sec('タイムゾーン', '日付表示にはタイムゾーンの考慮が必要です。ユーザーのタイムゾーンを検出し、UTC で保存・ローカルで表示するのがベストプラクティスです。\n\ndate-fns-tz や Luxon でタイムゾーン変換を行います。'),
      ]),
      lesson('pluralization', '複数形の処理', '言語ごとの複数形ルールを学びます', [
        sec('複数形の複雑さ', '英語は one/other の2形、日本語は複数形の区別なし、ロシア語は3形、ポーランド語は4形——言語ごとに複数形ルールが異なります。i18n ライブラリがこの複雑さを吸収します。', { code: '// locales/en.json\n{\n  "items": "{{count}} item",\n  "items_plural": "{{count}} items"\n}\n\n// locales/ja.json\n{ "items": "{{count}}件" }\n\nt("items", { count: 5 })  // en: "5 items", ja: "5件"' }),
        sec('ICU MessageFormat', 'i18next の ICU プラグインや next-intl の組み込みサポートで、複雑な複数形・性・条件分岐を1つのキーで表現できます。', { code: '{count, plural, =0 {No items} one {# item} other {# items}}' }),
        sec('性（Gender）', '一部の言語では名詞の性によって形容詞や動詞が変化します。ICU MessageFormat の select 構文で対応します。'),
      ]),
      lesson('routing', '多言語ルーティング', 'URL ベースのロケール切り替えを学びます', [
        sec('URL パターン', 'サブパス（/ja/about、/en/about）、サブドメイン（ja.example.com）、クエリパラメータ（?lang=ja）の3パターンがあります。サブパスが SEO に最も有利です。', { code: '// Next.js App Router\n// app/[locale]/about/page.tsx → /ja/about, /en/about\n\n// 言語切り替えリンク\nimport { Link } from "next-intl";\n<Link href="/about" locale="en">English</Link>' }),
        sec('ロケール検出', 'Accept-Language ヘッダー、Cookie、URL パスの優先順位でロケールを決定します。next-intl のミドルウェアが自動処理します。\n\nユーザーが明示的に言語を選択した場合は Cookie に保存し、次回以降その言語を優先します。'),
        sec('hreflang タグ', 'SEO 用に各言語版の URL を link rel="alternate" hreflang で指定します。next-intl や next-sitemap で自動生成できます。'),
      ]),
      lesson('rtl', 'RTL 対応', '右から左の言語への対応を学びます', [
        sec('RTL とは', 'アラビア語、ヘブライ語、ペルシア語などは右から左（RTL）に読みます。dir="rtl" を html 要素に設定し、レイアウトを反転させます。'),
        sec('CSS Logical Properties', 'margin-left/right の代わりに margin-inline-start/end を使い、LTR/RTL 両方で正しく動作するスタイルを書きます。', { code: '/* 物理プロパティ（RTL で問題） */\nmargin-left: 16px;\n\n/* 論理プロパティ（RTL 対応） */\nmargin-inline-start: 16px;\n\n/* Tailwind CSS */\n<div class="ms-4 me-2">  /* margin-inline-start, margin-inline-end */' }),
        sec('テスト', 'RTL モードでレイアウトを確認し、テキストの切り詰め、ボタンの位置、アイコンの向きが正しいか検証します。Storybook の RTL アドオンが便利です。', { tip: 'デモで dir="rtl" を設定し、レイアウトが反転することを確認してみてください。' }),
      ]),
      lesson('workflow', '翻訳ワークフロー', '翻訳の管理と抽出を学びます', [
        sec('翻訳ファイルの管理', 'JSON や YAML で翻訳を管理し、Git でバージョン管理します。翻訳者はコードを触らず、翻訳ファイルだけを編集できます。\n\nCrowdin、Lokalise、Phrase などの翻訳管理プラットフォーム（TMS）と連携すると、翻訳者との協業がスムーズになります。'),
        sec('未翻訳キーの検出', 'i18next-parser や FormatJS CLI でコードから翻訳キーを自動抽出し、翻訳ファイルを更新します。CI で未翻訳キーをチェックするスクリプトも有効です。'),
        sec('翻訳の品質', '機械翻訳（Google Translate API）を初期翻訳に使い、人間がレビュー・修正するハイブリッド方式が効率的です。文脈に依存する UI テキストは人間の翻訳が必須です。'),
      ]),
      lesson('next-steps', '次のステップ', 'i18n の学習を続けるための道筋を確認します', [
        sec('次に学ぶこと', '① SEO 入門 — 多言語 SEO\n② Storybook 入門 — 多言語ストーリー\n③ CSSアニメーション — RTL アニメーション\n④ Next.js — App Router の国際化'),
        sec('学習の道筋', 'i18n は最初から設計に組み込むのが鉄則です。後から追加すると全 UI テキストの置き換えが必要になり、コストが膨大になります。新規プロジェクトでは最初のコミットから i18n を導入しましょう。\n\nおめでとうございます！i18n 入門をすべて学びました 🎉', { tip: '既存プロジェクトの主要画面に next-intl を導入し、日本語と英語を切り替えてみてください。' }),
        sec('実践チェックリスト', '• UI テキストをハードコードしていないか\n• 日付・数値を Intl API でフォーマットしているか\n• URL ベースのロケール切り替えを実装しているか\n• hreflang タグを設定しているか\n• RTL 対応を考慮しているか'),
      ]),
    ],
  },
  {
    slug: 'animation',
    componentName: 'AnimationTutorial',
    courseTitle: 'CSSアニメーション',
    sidebarTitle: 'CSSアニメーション チュートリアル',
    heroAccent: 'CSS Animation',
    heroSub: 'transition、transform、keyframes、Framer Motion まで。心地よい UI モーションを10レッスンで学びます。',
    feature1: 'アニメーションプリセットをデモでリアルタイム確認できます。',
    icon: '✨',
    logoIcon: '✨',
    section: 'practice',
    subtitle: 'UI モーション',
    description: 'CSS transition、transform、keyframes、タイミング関数、CSS 変数、パフォーマンス、Framer Motion、アクセシビリティまで。10レッスンで学びます。',
    gradient: 'linear-gradient(135deg, #f472b6, #c026d3)',
    lessons: [
      lesson('intro', 'UI アニメーションとは？', 'モーションの役割と原則を理解します', [
        sec('なぜアニメーションか', 'アニメーションは装飾ではなく、UI のフィードバック、状態変化の伝達、空間的な関係の理解を助ける重要な要素です。ボタンを押した感触、ページ遷移の方向性、要素の出現・消失——これらがないと UI は「硬い」と感じられます。\n\nただし過度なアニメーションは逆効果です。「意味のあるモーション」を心がけましょう。'),
        sec('アニメーションの原則', '①目的がある——フィードバック、注意喚起、つながりの表現。②速い——200-300ms が UI アニメーションの標準。③自然——イージングで物理的な動きを再現。④控えめ——常にアニメーションしている UI は疲れる。', { tip: 'デモでアニメーションあり/なしの UI を比較し、体験の違いを確認してみてください。' }),
        sec('CSS vs JavaScript', 'CSS アニメーション（transition、keyframes）は GPU アクセラレーションが効き、パフォーマンスが良い。JavaScript（Framer Motion、GSAP）は複雑なシーケンスや物理演算に向いています。\n\nまず CSS で実現できないか検討し、必要なら JS ライブラリを使うのが基本です。'),
      ]),
      lesson('transitions', 'CSS Transition', 'プロパティの滑らかな変化を学びます', [
        sec('基本構文', 'transition はプロパティの変化を滑らかにアニメーションします。hover、focus、クラス切り替えで状態変化を表現します。', { code: '.button {\n  background: #3b82f6;\n  transition: background 0.2s ease, transform 0.15s ease;\n}\n.button:hover {\n  background: #2563eb;\n  transform: translateY(-2px);\n}\n.button:active {\n  transform: translateY(0);\n}' }),
        sec('transition プロパティ', 'transition-property（対象プロパティ）、transition-duration（時間）、transition-timing-function（イージング）、transition-delay（遅延）を指定します。\n\ntransition: all は全プロパティが対象になり、パフォーマンス問題を起こすことがあるため、具体的なプロパティを指定するのがベストプラクティスです。'),
        sec('複数プロパティの遷移', 'カンマ区切りで複数の transition を指定し、プロパティごとに異なる時間・イージングを設定できます。', { code: 'transition:\n  opacity 0.3s ease,\n  transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1),\n  background 0.15s ease;' }),
      ]),
      lesson('transforms', 'CSS Transform', '要素の変形を学びます', [
        sec('基本変形', 'translate（移動）、rotate（回転）、scale（拡大縮小）、skew（傾斜）で要素を変形します。transform はレイアウトを変更せず、GPU で処理されるためパフォーマンスが良いです。', { code: '.card:hover {\n  transform: translateY(-4px) scale(1.02);\n}\n\n.icon {\n  transition: transform 0.3s ease;\n}\n.icon:hover {\n  transform: rotate(90deg);\n}' }),
        sec('transform-origin', '変形の基準点を指定します。デフォルトは center ですが、左下基準の回転など、origin を変えると印象が大きく変わります。', { code: '.door {\n  transform-origin: left center;\n  transition: transform 0.5s ease;\n}\n.door.open {\n  transform: perspective(800px) rotateY(-80deg);\n}' }),
        sec('3D Transform', 'perspective、rotateX/Y/Z、translateZ で3D 効果を実現します。カードのフリップ、パララックス効果などに使います。\n\ntransform-style: preserve-3d で子要素の3D 空間を保持します。'),
      ]),
      lesson('keyframes', '@keyframes アニメーション', 'キーフレームによる複雑なアニメーションを学びます', [
        sec('@keyframes の定義', '開始（0%/from）から終了（100%/to）のキーフレームを定義し、animation プロパティで適用します。', { code: '@keyframes fadeInUp {\n  from {\n    opacity: 0;\n    transform: translateY(20px);\n  }\n  to {\n    opacity: 1;\n    transform: translateY(0);\n  }\n}\n\n.fade-in {\n  animation: fadeInUp 0.5s ease forwards;\n}' }),
        sec('animation プロパティ', 'animation-name、animation-duration、animation-timing-function、animation-delay、animation-iteration-count（回数/infinite）、animation-direction（alternate で往復）、animation-fill-mode（forwards で終了状態を保持）。'),
        sec('実用的なパターン', 'スピナー（回転）、パルス（拡大縮小の繰り返し）、シマー（ローディングスケルトン）、バウンス（弾む動き）など、よく使うパターンを @keyframes で定義し、ユーティリティクラスとして再利用します。', { code: '@keyframes spin {\n  to { transform: rotate(360deg); }\n}\n@keyframes pulse {\n  0%, 100% { opacity: 1; }\n  50% { opacity: 0.5; }\n}\n@keyframes shimmer {\n  0% { background-position: -200% 0; }\n  100% { background-position: 200% 0; }\n}' }),
      ]),
      lesson('timing', 'タイミング関数', 'イージングとモーションの自然さを学びます', [
        sec('イージングの種類', 'ease（加速→減速）、ease-in（加速）、ease-out（減速）、ease-in-out（加速→減速）、linear（等速）。UI 要素の出現は ease-out、消失は ease-in が自然です。'),
        sec('cubic-bezier', 'カスタムイージングを cubic-bezier(x1, y1, x2, y2) で定義します。cubic-bezier.com で視覚的に調整できます。', { code: '/* バウンス効果 */\ntransition: transform 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);\n\n/* スナップ効果 */\ntransition: transform 0.3s cubic-bezier(0.68, -0.55, 0.27, 1.55);' }),
        sec('spring アニメーション', 'CSS にはないバネのような動きは JavaScript ライブラリ（Framer Motion の spring）で実現します。stiffness（硬さ）と damping（減衰）でバネの挙動を調整します。\n\n物理ベースのモーションはユーザーに「生きている」感覚を与えます。', { tip: 'デモで異なるイージング関数の動きを比較してみてください。' }),
      ]),
      lesson('css-variables', 'CSS 変数とアニメーション', '動的なアニメーション制御を学びます', [
        sec('CSS 変数のアニメーション', 'CSS カスタムプロパティ（--variable）は transition でアニメーション可能です。テーマ切り替えやダイナミックな値の変化に使います。', { code: ':root {\n  --accent: #3b82f6;\n  --radius: 8px;\n  transition: --accent 0.3s ease;\n}\n[data-theme="dark"] {\n  --accent: #60a5fa;\n}\n.button {\n  background: var(--accent);\n  border-radius: var(--radius);\n}' }),
        sec('@property', '@property で CSS 変数の型を登録し、より滑らかなアニメーションを実現します。', { code: '@property --progress {\n  syntax: "<percentage>";\n  initial-value: 0%;\n  inherits: false;\n}\n.progress {\n  --progress: 0%;\n  background: conic-gradient(#3b82f6 var(--progress), #e5e7eb 0);\n  transition: --progress 0.5s ease;\n}' }),
        sec('Tailwind CSS', 'Tailwind の transition-*、animate-* ユーティリティでアニメーションを簡潔に適用できます。tailwindcss-animate プラグインで fade-in、slide-in 等のプリセットが使えます。'),
      ]),
      lesson('performance', 'パフォーマンス', 'GPU フレンドリーなアニメーションを学びます', [
        sec('GPU アクセラレーション', 'transform と opacity のアニメーションは GPU で処理され（composite レイヤー）、レイアウトやペイントを発生させません。width、height、top、left のアニメーションはレイアウト再計算を起こし、パフォーマンスが悪いです。', { code: '/* 悪い: レイアウト再計算 */\n.element { transition: width 0.3s; }\n\n/* 良い: GPU 合成 */\n.element { transition: transform 0.3s; }\n.element.expanded { transform: scaleX(1.5); }' }),
        sec('will-change', 'will-change: transform でブラウザに事前にレイヤー作成を指示します。ただし多用するとメモリを消費するため、アニメーション直前に設定し、終了後に削除します。'),
        sec('フレームレート', '60fps（16.7ms/frame）を維持するには、アニメーション中の JS 処理を最小化します。Chrome DevTools の Performance タブでフレームドロップを確認できます。\n\nprefers-reduced-motion メディアクエリで、アニメーションを減らす設定のユーザーにはアニメーションを無効化します。', { code: '@media (prefers-reduced-motion: reduce) {\n  *, *::before, *::after {\n    animation-duration: 0.01ms !important;\n    transition-duration: 0.01ms !important;\n  }\n}' }),
      ]),
      lesson('framer-motion', 'Framer Motion', 'React 向けアニメーションライブラリを学びます', [
        sec('基本', 'motion コンポーネントで宣言的にアニメーションを定義します。initial（初期状態）、animate（終了状態）、exit（アンマウント時）を指定します。', { code: 'import { motion } from "framer-motion";\n\n<motion.div\n  initial={{ opacity: 0, y: 20 }}\n  animate={{ opacity: 1, y: 0 }}\n  exit={{ opacity: 0, y: -20 }}\n  transition={{ duration: 0.3 }}\n>\n  Hello!\n</motion.div>' }),
        sec('layout アニメーション', 'layout プロパティで、レイアウト変更（リストの並べ替え、サイズ変更）を自動アニメーションします。', { code: '<motion.li layout transition={{ type: "spring", stiffness: 300 }}>\n  {item.text}\n</motion.li>' }),
        sec('AnimatePresence', 'コンポーネントのマウント/アンマウント時のアニメーションを制御します。モーダル、トースト、ページ遷移に必須です。', { code: 'import { AnimatePresence } from "framer-motion";\n\n<AnimatePresence>\n  {isOpen && (\n    <motion.div\n      initial={{ opacity: 0 }}\n      animate={{ opacity: 1 }}\n      exit={{ opacity: 0 }}\n    >\n      Modal content\n    </motion.div>\n  )}\n</AnimatePresence>' }),
      ]),
      lesson('accessibility', 'アクセシビリティ', 'アニメーションと a11y の両立を学びます', [
        sec('prefers-reduced-motion', 'OS の「視差効果を減らす」設定を尊重し、アニメーションを無効化または最小化します。これは WCAG 2.1 の要件です。', { code: 'const prefersReduced = window.matchMedia("(prefers-reduced-motion: reduce)").matches;\n\n<motion.div\n  animate={{ opacity: 1 }}\n  transition={{ duration: prefersReduced ? 0 : 0.3 }}\n/>' }),
        sec('アニメーションの注意点', '点滅（flash）はてんかんのトリガーになるため、1秒間に3回以上の点滅は避けます。自動再生するアニメーションは5秒以上で停止ボタンを提供します。\n\nアニメーション中もフォーカス可能な要素にアクセスできることを確認します。'),
        sec('意味のあるモーション', '装飾だけのアニメーションは reduced-motion ユーザーには不要です。状態変化のフィードバック（ボタン押下、フォーム送信成功）に集中し、純粋な装飾アニメーションは無効化可能にします。'),
      ]),
      lesson('next-steps', '次のステップ', 'アニメーションの学習を続けるための道筋を確認します', [
        sec('次に学ぶこと', '① Storybook 入門 — コンポーネントのアニメーション\n② SEO 入門 — CLS とアニメーション\n③ i18n 入門 — RTL アニメーション\n④ アクセシビリティ — モーション配慮'),
        sec('学習の道筋', '良いアニメーションは「気づかないけど、ないと寂しい」ものです。CSS transition から始め、keyframes で表現力を広げ、Framer Motion で React アプリに統合——段階的にスキルを積み上げましょう。\n\nおめでとうございます！CSSアニメーション入門をすべて学びました 🎉', { tip: '自分のプロジェクトのボタンやカードに hover アニメーションを追加してみてください。' }),
        sec('実践チェックリスト', '• transform/opacity を優先しているか\n• アニメーション時間は 200-300ms か\n• prefers-reduced-motion に対応しているか\n• 装飾的なアニメーションを控えめにしているか\n• 60fps を維持できているか'),
      ]),
    ],
  }
]